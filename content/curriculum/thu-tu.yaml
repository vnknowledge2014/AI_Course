# Thứ tự học — nguồn sự thật duy nhất về "bài nào đứng trước bài nào".
#
# Tên thư mục KHÔNG quyết định thứ tự. `nen-tang` đứng sau `onboarding` dù
# xếp trước theo bảng chữ cái, và không có quy ước đặt tên nào sửa được điều
# đó mà không làm hỏng thứ khác. Nên thứ tự phải được ghi ra tường minh.
#
# `tools/kiem_do_thi.py` đọc file này để kiểm: mọi skill một bài YÊU CẦU đều
# phải được một bài ĐỨNG TRƯỚC nó DẠY. Thiếu thứ tự thật thì phép kiểm ấy
# vô nghĩa.

realm:
  - id: onboarding
    ten: "Realm 0 — Bãi đáp của Byte"
    # Dùng `module` trong frontmatter (slug KHÔNG mang số thứ tự), không dùng
    # tên thư mục. Số thứ tự thuộc về đường dẫn; danh tính module thì không —
    # cùng lý do `id` bài học không mang số (xem content-compiler/src/cli.ts).
    track:
      - may-tinh-noi-gi
      - ra-lenh-cho-byte

  - id: nen-tang
    ten: "Realm 1 — Nền tảng lập trình"
    track:
      # T1.1 chưa viết; giữ chỗ để thứ tự không đổi khi nó xuất hiện.
      - gia-tri-bien-kieu
      - re-nhanh-va-lap
      - ham-vien-gach
      - list-dict-set-tuple
      - chuong-trinh-that

  # Realm 2 đi SONG SONG với Realm 1, không nối tiếp — người học vào được khi
  # chưa biết vòng lặp hay hàm. Xếp nó sau ở đây chỉ để cổng đồ thị có một thứ
  # tự tuyến tính mà kiểm; nó KHÔNG có nghĩa là phải học hết Realm 1 trước.
  - id: toan
    ten: "Realm 2 — Toán & Toán rời rạc"
    track:
      - cam-nhan-so
      - dai-so-va-ham-so
      - logic-va-chung-minh
      # T2.4 mượn `∈`/`{x|...}` lần đầu (T2.3 cố ý chưa dùng — xem "Ranh
      # giới track" cuối mục T2.3 trong content/toan/MACH.md), và dùng lại
      # `all()`/`any()` (T2.3) để kiểm phản xạ/đối xứng/bắc cầu trên mọi
      # cặp. Đứng ngay sau logic-va-chung-minh, không song song.
      - tap-hop-quan-he-anh-xa
      # T2.5 định nghĩa xác suất QUA tập hợp/tập con (T2.4) và biến ngẫu
      # nhiên QUA ánh xạ (T2.4 bài 24) — đứng ngay sau tap-hop-quan-he-anh-xa,
      # không song song. Xem "QUAN TRỌNG" đầu mục T2.5 trong MACH.md: track
      # KHÔNG dạy lại vòng lặp đếm/cộng dồn/tong-dem, đã đóng ở R1 (T1.2 bài
      # 14, 15, 27).
      - to-hop-xac-suat-thong-ke
      # T2.6 định nghĩa đồ thị vô hướng QUA quan hệ đối xứng (T2.4 bài 16,
      # 20) — KHÔNG mượn gì từ R3 (khoa-hoc-may-tinh đứng SAU toan trong
      # chính file này, nên "Đồ thị tiền đề" sẽ không coi R3 là đã dạy tại
      # điểm này — xem "QUAN TRỌNG" đầu mục T2.6 trong MACH.md).
      - do-thi-modular-dai-so-truu-tuong
      # T2.7 mượn cặp có thứ tự (T2.4 bài 14) để tổng quát hoá lên vector,
      # và tái dùng hợp thành ánh xạ + phần tử đơn vị (T2.6 bài 23) cho ma
      # trận đơn vị/quy tắc dây chuyền. Đứng ngay sau do-thi-modular-dai-so-
      # truu-tuong, đóng track cuối cùng của Realm 2.
      - dstt-giai-tich-cho-ai

  # Realm 3 mở màn M8. T3.1 đứng đầu vì MASTERPLAN §9.1 giao cho nó việc "dọn
  # đường cho ownership" — người học gặp Rust ở R4.T4.0b, và mọi chỗ khó của
  # ownership đều quy về một câu hỏi T3.1 dựng lên: cái tên GIỮ giá trị, hay
  # nó chỉ CHỈ TỚI giá trị?
  #
  # Nó đứng sau Realm 1 vì mượn `core.list-aliasing`, `core.slice-copy`,
  # `core.argument-not-copy`; và sau Realm 2 vì mượn bảng vị trí của T2.1.
  - id: khoa-hoc-may-tinh
    ten: "Realm 3 — Khoa học máy tính"
    track:
      - bit-byte-va-bo-nho
      # T3.2 mượn `mem.address`, `mem.name-is-reference`, `mem.utf8-bytes`,
      # `mem.mutability` từ T3.1 (một nút liên kết CHÍNH LÀ tham chiếu T3.1
      # vừa dạy), và mượn `core.dict`/`core.string-immutable` từ R1 — đứng
      # ngay sau T3.1, không song song.
      - cau-truc-du-lieu
      # T3.3 đào sâu `func.recursion`/`func.call-stack` (R1.T1.3) bằng cách
      # nối chúng với `ds.stack` T3.2 vừa dựng, và dùng thẳng `ds.queue`,
      # `ds.graph-representation`, `ds.tree-traversal` của T3.2 cho BFS/DFS —
      # đứng ngay sau T3.2, không song song.
      - thuat-toan-va-do-phuc-tap
      # T3.4 dùng thẳng `ds.stack`/`ds.heap` (T3.2) và `alg.recursion-is-stack`/
      # `alg.recursion-limit`/`alg.count-recursive-calls` (T3.3) để mở nắp
      # tầng dưới: bytecode, fetch-decode-execute, và phân biệt "ngăn xếp/
      # đống CẤU TRÚC DỮ LIỆU" (T3.2) với "ngăn xếp/đống VÙNG BỘ NHỚ" (track
      # này) — đứng ngay sau T3.3, không song song.
      - may-chay-the-nao
      # T3.5 (track cuối Realm 3) dùng thẳng `ds.tree`/`ds.tree-traversal`
      # (T3.2), `alg.recursion-is-stack` (T3.3), `may.fetch-decode-execute`/
      # `may.eval-stack` (T3.4), và `mem.name-is-reference` (T3.1) để xây một
      # trình thông dịch nhỏ từ token tới cây tới đánh giá — đứng ngay sau
      # T3.4, không song song.
      - ngon-ngu-va-may-truu-tuong

  # Realm 4 mở màn M8 phần Functional Programming. T4.0a là CỔNG — 18 bài
  # dạy đúng một Ý (kiểu được kiểm TRƯỚC KHI CHẠY) bằng TypeScript, ngôn
  # ngữ ĐẦU TIÊN trong khoá ngoài Python. Đứng sau toàn bộ Realm 3 vì mượn
  # core.variable/core.function-def/core.function-parameter từ R1 — không
  # mượn gì từ R3, chỉ xếp sau R3 vì R3 vừa xong và đây là track kế tiếp
  # theo đúng thứ tự M8 (không có ràng buộc tiền đề bắt buộc phải sau R3).
  - id: lap-trinh-ham
    ten: "Realm 4 — Lập trình hàm"
    track:
      - cong-typescript
      # T4.0b mượn `mem.name-is-reference` thẳng từ T3.1 (câu hỏi "tên GIỮ
      # hay CHỈ TỚI giá trị?" mà ownership Rust đặt luật lên trên) — đứng
      # sau T4.0a vì đây là track kế tiếp theo đúng thứ tự M8 (không mượn
      # gì từ T4.0a, chỉ xếp sau vì T4.0a vừa xong).
      - cong-rust
      # T4.1 là track FP THẬT đầu tiên (T4.0a/T4.0b chỉ là cổng ngôn ngữ).
      # Mượn `mem.aliasing-explained`/`mem.mutability`/`mem.shallow-copy`
      # thẳng từ T3.1 — bài 1 dựng lại đúng vấn đề T3.1 đã nêu (nhiều tên
      # cùng trỏ một giá trị) rồi dạy KỶ LUẬT đối phó (không sửa tại chỗ).
      # Không mượn gì từ T4.0a/T4.0b, chỉ xếp sau vì đúng thứ tự M8.
      - bat-bien-thuan-khiet
      # T4.2 mượn `fp.pure-fn-def`/`fp.nondeterministic-impure` THẲNG từ
      # T4.1 (bài 14 dạy closure không thuần bằng đúng khái niệm T4.1 đã
      # dạy cho hàm; bài 26 nói composability đòi hỏi từng mắt xích thuần)
      # — đứng ngay sau T4.1, không song song.
      - ham-bac-cao-hop-thanh
      # T4.3 QUAY LẠI TypeScript — mượn THẲNG `ts.interface` từ T4.0a (bài 1
      # cần đúng cú pháp `interface` đó làm điểm xuất phát cho discriminated
      # union), không mượn gì từ T4.1/T4.2 (Python). Đứng sau T4.2 vì đúng
      # thứ tự M8, không vì ràng buộc tiền đề bắt buộc phải liền sau nó.
      - adt-pattern-matching
      # T4.4 (Generics · Traits · Type classes, Rust) BỊ HOÃN — đo thật qua
      # rustprobe.mjs xác nhận trait bound (<T: Trait>) và dyn Trait KHÔNG
      # parse được trong byte-rust, generic function top-level không unify
      # đúng — thành phần CỐT LÕI của track không dùng được, không có tập
      # con để né như T4.0b từng làm với move-check. Xem
      # docs/decisions/R4-T44-generics-traits-do-duoc.md. T4.5 xếp NGAY sau
      # T4.3, bỏ qua T4.4 — không mượn gì từ T4.4 (chưa tồn tại) hay T4.0b,
      # chỉ mượn THẲNG `ts.adt-gate-boss` từ T4.3 (bài 8 cần discriminated
      # union đã dạy làm nền cho Option/Result tự viết).
      - dai-so-cua-chuong-trinh
      # T4.6 (Parser combinators & interpreter, Rust) CŨNG BỊ HOÃN — đo
      # thật qua rustprobe.mjs xác nhận HAI lỗ hổng độc lập: closure gán
      # biến rồi gọi qua tên biến không hoạt động (không có cú pháp kiểu
      # nào cho tham số hàm là closure), VÀ bộ kiểm mượn/di chuyển từ chối
      # "chưa hỗ trợ" bất kỳ biến non-Copy nào bị chạm ở ≥2 nhánh if/else —
      # khuôn bắt buộc có ở mọi recursive-descent parser thật. Xem
      # docs/decisions/R4-T46-parser-combinator-do-duoc.md. Với CẢ T4.4 và
      # T4.6 hoãn, Realm 4 dừng ở 6/8 track (186/260 bài) cho tới khi có
      # đầu tư hạ tầng byte-rust riêng.

  # Realm 5 (Software Engineering) mở màn bằng T5.3 (DDD, 42/42 XONG), KHÔNG
  # theo thứ tự số T5.1→T5.6 — T5.1 ("Git, MỚI, git mô phỏng WASM offline")
  # đòi một ENGINE MỚI HOÀN TOÀN, không tồn tại trong `packages/`, ngoài
  # phạm vi một phiên viết nội dung. T5.3 có nguồn tham khảo RÕ NHẤT (7
  # chương trọn vẹn, `fp/TypeScript_Books/part_4_ddd/`) và chỉ cần engine
  # exec-typescript đã chứng minh ổn định. Xem `content/curriculum/
  # r5-t53-xuong.yaml` cho chi tiết quyết định. T5.3 mượn `ts.adt-gate-boss`
  # (T4.3) và `alg.gate-boss` (T4.5) — đứng sau TOÀN BỘ Realm 4 vì cần cả hai.
  #
  # T5.6 (Bảo mật ứng dụng, 26 bài) là track THỨ HAI — cùng lý do như T5.3:
  # nguồn tham khảo RÕ NHẤT còn lại (2 chương trọn vẹn,
  # `fp/TypeScript_Books/part_7_production/chapter_39_security_essentials.md`
  # + `chapter_40_app_security_hardening.md`), engine exec-typescript ĐÃ ổn
  # định. Xem `content/curriculum/r5-t56-xuong.yaml`. T5.6 mượn
  # `ts.adt-gate-boss` (T4.3, bài 1) và `alg.gate-boss` (T4.5, bài 13) —
  # KHÔNG phụ thuộc T5.3 (hai track song song trong Realm 5, không thứ tự
  # bắt buộc giữa chúng).
  #
  # T5.2 (TDD→Property→Mutation, 34 bài) là track THỨ BA — 2/3 nguồn có sẵn
  # (`fp/TypeScript_Books/part_6_testing_fullstack/chapter_31_tdd_typescript.md`
  # + `chapter_32_property_based_testing.md`), 1/3 (Mutation, cụm 6, bài
  # 27-34) TỰ THIẾT KẾ vì KHÔNG chương sách nào trong `fp/*_Books/` nhắc
  # mutation testing — dựa TRỰC TIẾP trên kỷ luật `tools/kiem_dot_bien.mjs`
  # dự án đã dùng THẬT xuyên suốt T5.3+T5.6. Xem `content/curriculum/
  # r5-t52-xuong.yaml`. T5.2 mượn `ts.adt-gate-boss` (T4.3, bài 1) —
  # KHÔNG phụ thuộc T5.3/T5.6 (ba track song song trong Realm 5).
  #
  # T5.4 (Mẫu thiết kế & Tái cấu trúc, 24 bài) là track THỨ TƯ — nguồn
  # `fp/TypeScript_Books/part_3_design_patterns/chapter_16_gof_fp_translation.md`
  # + `chapter_17_cqrs_event_sourcing.md`. Xem `content/curriculum/
  # r5-t54-xuong.yaml`. T5.4 mượn `ts.adt-gate-boss` (T4.3, bài 1) —
  # KHÔNG phụ thuộc T5.2/T5.3/T5.6 (bốn track song song trong Realm 5).
  #
  # T5.5 (CI/CD & Observability, 24 bài) là track THỨ NĂM — nguồn
  # `fp/TypeScript_Books/part_7_production/chapter_43_capstone_production.md`
  # §43.4-43.5 (chỉ ~120 dòng, phần LỚN track TỰ THIẾT KẾ). Xem
  # `content/curriculum/r5-t55-xuong.yaml`. Bài 1 mượn `mau.gate-boss-cqrs-es`
  # (T5.4, bài CUỐI) — VÌ T5.5 TÁI DÙNG kỹ thuật pipeline/composition ĐÃ học
  # Ở T5.4, nên xếp SAU T5.4 thay vì song song.
  - id: ky-nghe-phan-mem
    ten: "Realm 5 — Kỹ nghệ phần mềm"
    track:
      - ddd
      - bao-mat-ung-dung
      - kiem-thu
      - mau-tai-cau-truc
      - van-hanh

  # Realm 6 (MASTERPLAN §9.2) — 22 quest, 3 mốc phát hành độc lập (R6-1/R6-2/
  # R6-3), ★ LÃNH THỔ RUST (đa số quest học `cong-rust` T4.0b đã dạy). Mỗi
  # quest = một track/module riêng (thư mục riêng), khác Realm 1-5 nơi mỗi
  # track là một T-number lớn — quest ở đây nhỏ hơn (6-14 bài) nên ánh xạ
  # 1 quest = 1 track/thư mục cho vừa quy mô.
  #
  # `tier: static` (requireAst) KHÔNG dùng được cho bước code Rust/TypeScript
  # — `tools/kiem_ma_bai_hoc.mjs` ném lỗi cứng nếu khai (xác nhận bằng đọc mã
  # nguồn, dòng ~420-435: TsAstKind/RsAstKind CHƯA cài nhánh xử lý nào). Mọi
  # bài Rust/TS ở Realm 6 dựa HOÀN TOÀN vào tier `run`/`tests`/`output`, bù
  # bằng test suite nhiều ca biên hơn. Chỉ bài Python (q00, có thể q04/q10
  # nếu xác nhận là TS thì cũng áp dụng) mới dùng được `requireAst`.
  #
  # Mọi bài storage lập trình trên `packages/simdisk` (đã cài, xem README ở
  # đó) — KHÔNG `open()` thật, vì Pyodide MEMFS không mô phỏng được
  # crash/torn-write. Bài học chép phần SimDisk cần vào `readonly`, không
  # import gói workspace (sandbox chấm không nạp được).
  - id: co-so-du-lieu
    ten: "Realm 6 — Database từ số 0"
    track:
      # R6-1 (68 bài) — nền lưu trữ một máy: byte/file → log → WAL/recovery →
      # B+Tree → LSM → khoá/2PL → MVCC.
      - chiec-hop-giay-cua-byte
      # q01 mượn thẳng khái niệm "file record cố định" (q00) rồi đổi sang
      # append-only + hash index (Bitcask) — đứng ngay sau q00.
      - nhat-ky-khong-bao-gio-quen
      # q02 dùng thẳng "append-only log" (q01) làm WAL, thêm fsync/torn
      # write/CRC32/recovery — đứng ngay sau q01, cần packages/simdisk.
      - khi-dien-mat
      # q03 (B+Tree trên page 4KB) độc lập với log — nhưng đứng sau q02 vì
      # cùng track R6-1, dùng lại kỷ luật fsync/recovery cho split/merge an
      # toàn khi crash giữa chừng.
      - cay-cua-nguoi-thu-thu
      # q04 [TS] — LSM (skiplist, SSTable, bloom, compaction) đối chiếu với
      # B+Tree (q03) vừa học — Bước Rosetta đầu tiên của Realm 6 (ngôn ngữ
      # phụ). Đứng ngay sau q03 để so sánh trực tiếp còn nóng.
      - thac-du-lieu
      # q05 (lock table, 2PL, deadlock detection) cần một storage engine đã
      # có (B+Tree q03 hoặc LSM q04) làm nơi khoá — đứng sau cả hai.
      - bon-chu-cai-acid
      # q06 (MVCC, snapshot isolation, SSI, ARIES-lite) mở rộng khoá q05
      # thành "nhiều dòng thời gian" — đứng ngay sau q05, đóng R6-1.
      - nhieu-dong-thoi-gian

      # R6-2 (96 bài) — từ một máy lên "một hộp nhiều hình": ngôn ngữ truy
      # vấn → executor → index nâng cao → SurrealDB thật → phân tán.
      # q07 (lexer + Pratt parser + AST mini-SurrealQL) không phụ thuộc R6-1
      # trực tiếp — nhưng đứng sau vì đây là quest kế tiếp theo đúng thứ tự
      # M8/MASTERPLAN, và cần một storage engine (q03/q04) làm đích thực thi.
      - ngon-ngu-cua-byte
      # q08 (logical plan, pushdown, Volcano executor) dùng thẳng AST (q07)
      # làm đầu vào — đứng ngay sau q07.
      - bo-nao-cua-truy-van
      # q09 (key-encoding có thứ tự, secondary index, graph edge, HNSW-lite)
      # mở rộng executor (q08) với nhiều kiểu chỉ mục hơn — đứng ngay sau q08.
      - nhung-loi-tat-cua-byte
      # q10 (SurrealDB THẬT qua @surrealdb/wasm — nhiều khả năng [TS], xác
      # nhận khi viết) đối chiếu mini-SurrealQL tự viết (q07-q09) với bản
      # thật — đứng ngay sau q09, đóng vòng "một hộp nhiều hình".
      - mot-hop-nhieu-hinh
      # q11 (murmur3, consistent hashing, vnode) mở đầu nhánh phân tán —
      # không phụ thuộc q07-q10 trực tiếp, đứng sau vì đúng thứ tự quest.
      - vong-tron-quyen-luc
      # q12 (shard-per-core, share-nothing) dùng thẳng vnode/hashing (q11)
      # để chia dữ liệu theo lõi — đứng ngay sau q11.
      - moi-loi-mot-vuong-quoc
      # q13 (RF/CL quorum, hinted handoff, read repair, Merkle) cần dữ liệu
      # đã phân tán (q11-q12) để có gì mà đồng bộ — đứng ngay sau q12.
      - dem-phieu
      # q14 (tombstone, gc_grace, STCS/LCS/TWCS) dọn dẹp cho đúng mô hình
      # LSM (q04) + phân tán (q11-q13) — đứng ngay sau q13.
      - bia-mo-va-nguoi-don-rac
      # q15 (wide-column, partition key, hot partition) tổng kết mô hình hoá
      # dữ liệu phân tán — đứng cuối R6-2, dùng mọi khái niệm q11-q14.
      - mo-hinh-hoa-theo-cau-hoi

      # R6-3 (63 bài) — "TigerBeetle nhỏ": ledger tất định, mô phỏng lỗi,
      # đồng thuận, capstone.
      # q16 (double-entry, 128B DataView, two-phase, linked) mở track mới,
      # dùng thẳng B+Tree/WAL (R6-1) làm nơi lưu bút toán — đứng ngay sau
      # R6-2 vì đúng thứ tự quest, không phụ thuộc trực tiếp R6-2.
      - so-cai-khong-sai-mot-xu
      # q17 (event loop tất định, xorshift128+, clock ảo) xây nền mô phỏng
      # cho ledger (q16) chạy lặp lại được — đứng ngay sau q16.
      - vu-tru-tat-dinh
      # q18 (simulated network + SimDisk fault, mini-VOPR) tiêm lỗi vào nền
      # tất định (q17) — đứng ngay sau q17, mở rộng packages/simdisk với
      # fault injection (torn write, lost fsync, latent sector error,
      # misdirected write, crash tất định thứ N).
      - ke-pha-hoai-co-chu-dich
      # q19 (VSR: prepare/prepare_ok/commit, view change) cần nền tất định
      # + tiêm lỗi (q17-q18) để kiểm đồng thuận sống sót qua lỗi — đứng ngay
      # sau q18.
      - dong-thuan-kieu-vsr
      # q20 "Phòng thí nghiệm thật" — ONLINE, desktop-only, NGOÀI mọi mốc bắt
      # buộc (ghi rõ trong MASTERPLAN §9.2) — xếp vị trí này để giữ đúng thứ
      # tự quest, nhưng có thể hoãn giống T4.4/T4.6 nếu hạ tầng desktop-only
      # chưa sẵn sàng lúc viết tới đây.
      - phong-thi-nghiem-that
      # q21 ByteLedger Capstone ghép TRỌN R6-3 (double-entry + tất định +
      # tiêm lỗi + VSR) — đứng cuối, đóng Realm 6.
      - byteledger-capstone

  # Realm 7 (System Design) — MASTERPLAN CHỈ có breakdown cấp TRACK (4 track,
  # 120 bài: T7.1 Nền tảng phân tân 30, T7.2 Thiết kế thực chiến 34, T7.3
  # System Design FOR Functional 30 [~70% MỚI, skill sd.fp.*], T7.4 Triển
  # khai & vận hành 26) — KHÔNG có "Chi tiết §9.x" cấp quest như R6's §9.2.
  # Thư mục `content/thiet-ke-he-thong/` (Vietnamese, theo ĐÚNG tiền lệ MỌI
  # realm đã viết — `co-so-du-lieu`/`lap-trinh-ham`/`ky-nghe-phan-mem` — dù
  # MASTERPLAN's cây thư mục §10 ghi tên gốc tiếng Anh CHƯA dịch
  # `system-design/` từ lúc còn là roadmap stub).
  #
  # Nguồn tham khảo: `references/repos/system-design-notes/` (ghi chú 28
  # chương sách System Design Interview — Alex Xu, Vol 1+2) — DÙNG làm bản
  # đồ khái niệm/đánh đổi (concept map), KHÔNG sao chép văn xuôi — mọi bài
  # viết THEO đúng khuôn `.lesson.md` mô phỏng-tương-tác của dự án (giống
  # R6 dùng SimDisk/VSR, không phải văn xuôi diễn giải sách).
  #
  # T7.1 (Nền tảng phân tán, 30 bài, 3 quest ~10 bài/quest) mở màn Realm 7 —
  # CỐ Ý TRÁNH lặp lại consistent hashing (q11, R6-2) VÀ RF/CL quorum+hinted
  # handoff+read repair+Merkle (q13, R6-2) đã dạy SÂU — ba quest dưới ĐI vào
  # đất MỚI hoàn toàn không chạm hai chủ đề đó.
  - id: thiet-ke-he-thong
    ten: "Realm 7 — Thiết kế hệ thống"
    track:
      # q7.1a (back-of-envelope estimation, horizontal/vertical scaling,
      # load balancing — ĐỊNH TUYẾN yêu cầu, khác hẳn q11's ĐỊNH TUYẾN dữ
      # liệu qua vòng băm —, CDN/cache-aside/write-through/LRU eviction) mở
      # màn T7.1 — không phụ thuộc trực tiếp bài nào của R6, chỉ đứng sau
      # R6 vì đúng thứ tự realm.
      - do-truoc-khi-thiet-ke
      # q7.1b (rate limiting: token/leaky bucket, fixed/sliding window log,
      # sliding window counter; unique ID: Snowflake bit-packing) — đứng
      # ngay sau q7.1a, tái dùng kỹ thuật ĐO/ước lượng (q7.1a) để chọn
      # tham số rate limiter thực tế.
      - dinh-danh-va-toc-do
      # q7.1c (URL shortener: base62 encoding + va chạm; web crawler: BFS,
      # politeness/robots.txt mô phỏng, dedup qua URL-frontier) — quest
      # TỔNG HỢP đóng T7.1, ráp load balancing (q7.1a) + rate limiting
      # (q7.1b) vào MỘT dịch vụ nhỏ hoàn chỉnh.
      - url-rut-gon-va-thu-thap-web
      # T7.2 "Thiết kế thực chiến" (34 bài, 4 quest) — MASTERPLAN chỉ nêu
      # DANH SÁCH case study cấp track (notification, news feed, chat,
      # search autocomplete, YouTube, Google Drive, proximity service,
      # message queue, metrics/monitoring, hotel reservation, distributed
      # email, S3-like storage, gaming leaderboard, payment, digital
      # wallet, stock exchange — 16 hệ thống), KHÔNG có breakdown quest —
      # TỰ nhóm theo chủ đề kỹ thuật chung (không theo thứ tự sách):
      - noi-dung-va-kham-pha
      # q7.2a — News Feed (fan-out on write/read), Search Autocomplete
      # (trie/prefix + top-K), YouTube (transcode pipeline + CDN) — chủ đề
      # chung: phục vụ NỘI DUNG lớn cho nhiều người đọc, đánh đổi
      # write-time vs read-time.
      - thoi-gian-thuc-va-vi-tri
      # q7.2b — Chat System (WebSocket/presence/delivery), Notification
      # System (multi-kênh + dedup), Proximity Service (geohash/quadtree),
      # Gaming Leaderboard (sorted set thời gian thực) — chủ đề chung: độ
      # trễ THẤP, cập nhật LIÊN tục, không phải batch.
      - ha-tang-du-lieu-quy-mo-lon
      # q7.2c — Distributed Message Queue (partition/offset/ack),
      # Metrics Monitoring & Alerting (time-series aggregation), Google
      # Drive (chunked upload/sync/conflict), S3-like Object Storage
      # (durability/erasure coding), Distributed Email (mailbox
      # storage+delivery queue) — chủ đề chung: HẠ TẦNG nền cho hệ thống
      # khác dùng, không phải sản phẩm người dùng cuối trực tiếp thấy.
      - giao-dich-va-tien
      # q7.2d — Hotel Reservation (inventory + double-booking), Payment
      # System (idempotency + ledger), Digital Wallet, Stock Exchange
      # (matching engine + order book) — chủ đề chung: TÍNH đúng đắn giao
      # dịch quan trọng HƠN tốc độ, dọn đường trực tiếp cho T7.3 (system
      # design for functional — idempotency/exactly-once như tính chất
      # đại số). Bốn quest × ~8-9 bài + BOSS ≈ 34 bài, viết TUẦN tự, xác
      # minh độc lập từng quest TRƯỚC khi sang quest kế — HOÀN TẤT.
      #
      # T7.3 "System Design for Functional" (30 bài, ~70% MỚI theo
      # MASTERPLAN) — namespace RIÊNG `sd.fp.*` (KHÔNG dùng `sd.*` như
      # T7.1/T7.2). MASTERPLAN cho khung khái niệm cấp TRACK (không có
      # breakdown quest) — TỰ thiết kế 3 quest ~10 bài, phủ đủ 5 khái
      # niệm MASTERPLAN liệt kê:
      - nhat-ky-bat-bien-va-fold
      # q7.3a — state = fold(events) trên một log bất biến (append-only,
      # không sửa/xoá); snapshot để tránh fold lại từ đầu; idempotency
      # VÀ exactly-once XÂY LẠI như tính chất ĐẠI SỐ (áp dụng event 2
      # lần = áp dụng 1 lần, khác hẳn cách tiếp cận Map<khoá,kết quả>
      # OOP của q7.2d bài3 — cùng Ý tưởng, khung hoàn toàn khác);
      # functional core (hàm thuần quyết định/áp dụng) / imperative
      # shell (I/O) Ở cấp DỊCH VỤ. Không phụ thuộc trực tiếp bài nào
      # của T7.2, chỉ đứng sau vì đúng thứ tự track — bài 1
      # `requires: [sd.boss-giao-dich-va-tien]`.
      - crdt-va-hop-nhat
      # q7.3b — CRDT = commutative monoid: hợp nhất (merge) từ NHIỀU
      # bản sao PHẢI giao hoán + kết hợp + có phần tử trung hoà để hội
      # tụ đúng bất kể thứ tự nhận — G-Counter, PN-Counter, G-Set/
      # OR-Set, LWW-Register; verify CẢ ba tính chất đại số (idempotent/
      # commutative/associative) bằng mini-test tự viết, không chỉ test
      # happy-path. Tái dùng khái niệm monoid ĐàHỌC Ở R4 T4.5 (Đại số
      # của chương trình) NHƯNG áp dụng Ở quy mô PHÂN TÁN, không phải
      # trong một tiến trình.
      - streaming-va-hieu-ung-o-bien
      # q7.3c — streaming/backpressure/effect system CHỈ Ở BIÊN (đọc/
      # ghi thật), LÕI vẫn thuần: windowing/batching qua fold thuần
      # (tái khung ý tưởng gom-theo-khung-thời-gian của q7.2c bài3,
      # NHƯNG viết lại độc lập theo lối functional core/imperative
      # shell); backpressure như một phép quyết định thuần (buffer đầy
      # → tín hiệu chậm lại) tách biệt khỏi cơ chế I/O thật thi hành nó.
      # BOSS đóng T7.3 ráp: log bất biến + CRDT merge + streaming pipeline
      # thành một luồng ingest nhỏ hoàn chỉnh.
      #
      # T7.4 "Triển khai & vận hành" (26 bài) — MASTERPLAN chỉ cho SỐ LƯỢNG
      # (26), KHÔNG có mô tả nội dung nào (khác T7.3 vốn có 5 khái niệm liệt
      # kê sẵn) — TỰ thiết kế HOÀN TOÀN 3 quest theo đúng chủ đề chuẩn của
      # mảng "deployment & operations" trong system design (điều những gì
      # T7.1-T7.3 CHƯA chạm: launch AN TOÀN, QUAN SÁT hệ thống đang chạy,
      # CHỐNG CHỊU lỗi hạ nguồn) — quay LẠI namespace `sd.*` (KHÔNG `sd.fp.*`,
      # vì đây không phải một track "reframe theo FP" như T7.3, mà là track
      # vận hành chung, viết theo lối OOP-mutation như T7.1/T7.2) — bài 1
      # bắc cầu từ T7.3 bằng `requires: [sd.fp.boss-streaming-va-hieu-ung-o-
      # bien]` (không thêm tiền tố `fp.` cho CHÍNH lesson này, giống hệt
      # cách q7.3a bắc cầu ngược từ T7.2).
      - trien-khai-an-toan
      # q7.4a (9 bài) — LAUNCH an toàn: rolling/blue-green/canary deployment
      # (đánh đổi downtime/rủi ro/tài nguyên), feature flag (bật/tắt tính
      # năng độc lập với deploy, targeting theo segment), health/readiness
      # probe (không route traffic tới instance chưa sẵn sàng), database
      # migration an toàn (expand-contract, backward-compatible schema),
      # load test trước khi launch, rollback TỰ ĐỘNG theo ngưỡng lỗi. BOSS
      # ráp canary + feature flag + auto-rollback thành một pipeline triển
      # khai hoàn chỉnh.
      - quan-sat-va-canh-bao
      # q7.4b (9 bài) — QUAN SÁT hệ thống đang chạy: ba trụ cột logs/
      # metrics/traces (khi nào dùng cái nào), structured logging, đúng
      # loại metric (counter/gauge/histogram) cho đúng câu hỏi, distributed
      # tracing (span/trace ID xuyên nhiều service), SLI/SLO/error budget
      # (định nghĩa "đủ tốt" VÀ ngân sách rủi ro được PHÉP tiêu), alert
      # theo TRIỆU CHỨNG không theo NGUYÊN NHÂN (tránh alert fatigue),
      # on-call + runbook, blameless postmortem. BOSS ráp toàn bộ
      # observability stack cho một sự cố mô phỏng, từ phát hiện tới
      # postmortem.
      - chong-chiu-va-khoi-phuc
      # q7.4c (8 bài, BOSS ĐÓNG R7) — CHỐNG CHỊU lỗi hạ nguồn: circuit
      # breaker (mở mạch tránh cascading failure), retry+backoff+jitter
      # (tránh thundering herd), bulkhead (cô lập tài nguyên), graceful
      # degradation (giảm chất lượng có kiểm soát thay vì sập), rate
      # limiting BẢO VỆ downstream (khác hướng với q7.1b's rate limit bảo
      # vệ CHÍNH hệ thống), backup/restore (RPO/RTO, verify backup THẬT
      # SỰ khôi phục được), disaster recovery/multi-region failover. BOSS
      # ráp circuit breaker + retry + bulkhead + graceful degradation vào
      # MỘT service mô phỏng chịu lỗi hạ nguồn, ĐÓNG R7 hoàn toàn
      # (120/120) — có thể điểm lại (không viết lại) vài khái niệm xuyên
      # suốt R7 nếu hợp ngữ cảnh tổng kết.
      #
      # 9+9+8 = 26, khớp đúng số MASTERPLAN. R7 sau T7.4: 120/120 — ĐÓNG
      # HOÀN TOÀN Realm 7 "Thiết kế hệ thống".

  # Realm 8 (MASTERPLAN.md §9 "R8 AI · GENERATIVE AI · RAG", [roadmap], 6
  # track · 190 lesson: T8.1 Máy học nền tảng 30, T8.2 Mạng nơ-ron từ số 0 32
  # (autograd ~200 dòng), T8.3 Transformer từ số 0 40 (BPE tay → attention
  # tính tay trên 4 token → micro-transformer 2 tầng numpy, corpus 5KB, chạy
  # <2s; lab TUỲ CHỌN GPT-2 đầy đủ KHÔNG chấm), T8.4 Dùng LLM đúng cách 28,
  # T8.5 RAG từ số 0 34 (HNSW tự cài, BM25 hybrid), T8.6 Hạ tầng & vận hành
  # AI 26) — KHÔNG có "Chi tiết §9.x" cấp quest (giống T7.3/T7.4, khác R6's
  # §9.2) — TỰ thiết kế quest breakdown TRONG từng track khi tới lượt, một
  # track tại một thời điểm (giống cách R7 đã làm cho T7.3/T7.4).
  #
  # ★ LÃNH THỔ PYTHON — "numpy thuần trong Pyodide, KHÔNG torch" (MASTERPLAN
  # §9.1). Hạ tầng vendor+nạp numpy (wheel WASM xác thực sha256, nạp một lần
  # lúc khởi động Pyodide, cả app thật lẫn hai cổng chấm) ĐÃ xong VÀ verify
  # thật TRƯỚC khi viết bài đầu tiên (đúng mẫu "T4.0b Cổng Rust" — hạ tầng
  # trước nội dung) — xem commit riêng ngay trước entry này trong git log.
  #
  # Thư mục: `content/tri-tue-nhan-tao/` (Vietnamese, ĐÚNG tiền lệ mọi realm
  # đã viết — MASTERPLAN's cây thư mục §10 ghi `ai-genai-rag/` CHỈ LÀ tên gốc
  # tiếng Anh CHƯA dịch từ lúc còn LÀ roadmap stub, giống hệt tình huống
  # `system-design/` → `thiet-ke-he-thong/` ở R7).
  # `teaches`/`requires`/`concepts` namespace: `ai.<slug>` (MASTERPLAN §10
  # liệt kê `ai.concepts.yaml` cạnh `fp.concepts.yaml`/`db.concepts.yaml` —
  # xác nhận mỗi realm CÓ namespace riêng; `content/concepts/` bản thân
  # KHÔNG tồn tại trên đĩa — cũng LÀ trang trí roadmap chưa build, giống
  # `content/concepts/`/`content/skills/` nói chung — không cần đăng ký gì
  # thêm ngoài field `teaches`/`requires`/`concepts` trong frontmatter mỗi
  # bài, đúng cách R6/R7 đã làm).
  #
  # Nguồn tham khảo KHÁI NIỆM: `references/repos/ai-for-beginners/lessons/`
  # (Microsoft "AI for Beginners", giấy phép mở) — CHỈ dùng LÀM bản đồ khái
  # niệm/thứ tự sư phạm rộng, KHÔNG sao chép văn xuôi/ví dụ — mọi bài viết
  # THEO khuôn mô phỏng-tương-tác riêng (giống R6/R7), số liệu VÍ DỤ tự nghĩ
  # VÀ tự verify bằng `node`/Pyodide thật, không dùng lại ví dụ CỦA nguồn.
  #
  # T8.1 "Máy học nền tảng" (30 bài, 3 quest 10 bài/quest) mở màn Realm 8 —
  # dạy ML kinh điển THUẦN numpy (không sklearn): hồi quy → phân loại/đánh
  # giá → tổng quát hoá/hợp nhất mô hình. Bài 1 `requires: []` — mở đầu CẢ
  # Realm 8 (không tiền đề, giống bài 1 của R6/R7).
  - id: tri-tue-nhan-tao
    ten: "Realm 8 — AI · Generative AI · RAG"
    track:
      - hoi-quy-va-gradient-descent
      # q8.1a (10 bài) — hồi quy tuyến tính từ số 0 (dạng đóng least-squares
      # ĐỐI CHIẾU gradient descent, không phải chỉ MỘT cách), hàm mất mát
      # (MSE/MAE, vì sao bình phương chứ không trị tuyệt đối cho gradient
      # trơn), cơ chế gradient descent (learning rate quá lớn phân kỳ/quá
      # nhỏ hội tụ chậm — demo SỐ THẬT cả hai ca), đặc trưng đa thức (fit
      # đường cong bằng hồi quy tuyến tính trên đặc trưng mở rộng),
      # chuẩn hoá/standardize (VÌ SAO gradient descent cần — demo hội tụ
      # chậm hẳn khi thiếu), overfitting (đa thức bậc cao khớp nhiễu),
      # regularization L2/ridge (phạt trọng số lớn) VÀ L1/lasso (đẩy về
      # 0 — sparsity), train/val/test split (vì sao cần tách ba, không
      # phải hai), cross-validation (k-fold, ước lượng ổn định hơn one-
      # shot split). BOSS ráp: chuẩn hoá → fit có regularization → chọn
      # tham số qua validation → báo cáo metric trên test set CHƯA từng
      # chạm.
      - phan-loai-va-danh-gia
      # q8.1b (10 bài) — logistic regression (sigmoid, binary cross-
      # entropy, vì sao KHÔNG dùng MSE cho phân loại), ranh giới quyết
      # định (decision boundary, trực quan 2D), đa lớp (softmax, one-vs-
      # rest ĐỐI CHIẾU), confusion matrix (TP/FP/TN/FN — định nghĩa
      # chính xác, không suy luận), precision/recall/F1 (khi accuracy
      # NÓI DỐI — demo dữ liệu lệch lớp accuracy cao nhưng vô dụng), ROC/
      # AUC (đánh đổi ngưỡng), k-NN (phân loại theo khoảng cách, KHÔNG
      # có pha "huấn luyện"), decision tree đơn giản (entropy/information
      # gain, một lần chia), k-means (vòng lặp gán-cụm/cập nhật-tâm,
      # unsupervised — khác hẳn mọi bài trước ĐỀU supervised). BOSS ráp
      # pipeline phân loại đầy đủ: huấn luyện 2 mô hình khác nhau, so
      # sánh qua NHIỀU metric (không chỉ accuracy), chọn mô hình theo
      # đúng câu hỏi bài toán đặt ra.
      - tong-quat-hoa-va-hop-nhat
      # q8.1c (10 bài, BOSS ĐÓNG T8.1) — bias-variance tradeoff (demo số
      # thật: underfit/overfit/vừa-đủ trên CÙNG dữ liệu), bootstrap
      # resampling, bagging (trung bình nhiều mô hình bootstrap — giảm
      # variance, đo THẬT bằng số), boosting đơn giản (sửa lỗi tuần tự
      # có trọng số, kiểu AdaBoost thu nhỏ), hyperparameter tuning (grid
      # search nhỏ + validation curve), data leakage (lỗi kinh điển:
      # chuẩn hoá TRƯỚC khi tách train/test — rò rỉ thông tin, demo số
      # THẬT chênh lệch), xử lý lệch lớp (reweighting/oversampling), ỔN
      # ĐỊNH SỐ HỌC (log(0), tràn số trong softmax, vì sao phải trừ max
      # trước exp — lỗi THẬT ai code ML cũng gặp ít nhất một lần). BOSS
      # ráp pipeline ML đầy đủ: dữ liệu thô → làm sạch/chuẩn hoá → tách
      # tập → huấn luyện có regularization → tuning → đánh giá → ensemble
      # — ĐÓNG T8.1 (30/30), bàn giao nền tảng số học cho T8.2 (backprop/
      # autograd sẽ tái dùng CHÍNH khái niệm gradient descent đã học Ở
      # đây, nhưng tính đạo hàm TỰ ĐỘNG thay vì công thức đóng viết tay).
      #
      # T8.2 "Mạng nơ-ron từ số 0" (32 bài, 4 quest — MASTERPLAN chỉ cho số
      # lượng+mô tả 1 dòng "autograd ~200 dòng", KHÔNG có breakdown cấp quest
      # — TỰ thiết kế, tham khảo bản đồ khái niệm ở
      # `references/repos/ai-for-beginners/lessons/3-NeuralNetworks/`
      # (03-Perceptron, 04-OwnFramework — CHỈ lấy trình tự sư phạm: neuron
      # đơn → vì sao cần phi tuyến (XOR) → MLP → backprop = chain rule lặp
      # trên đồ thị tính toán; KHÔNG sao chép văn xuôi/notebook, tự viết ví
      # dụ + tự verify số bằng Pyodide). Kiến trúc autograd mô phỏng theo lối
      # "micrograd" (Value vô hướng, KHÔNG phải tensor — giữ đúng ràng buộc
      # "autograd ~200 dòng" của MASTERPLAN, không phình thành mini-PyTorch).
      - neuron-va-mang-nhieu-tang
      # q8.2a (8 bài) — neuron đơn (nối tiếp logistic regression T8.1b: cùng
      # công thức wx+b qua sigmoid, giờ gọi tên ĐÚNG "neuron"), hàm kích hoạt
      # (sigmoid/tanh/ReLU/leaky ReLU — công thức + đạo hàm, vì sao đạo hàm
      # cần TRƯỚC khi học backprop ở q8.2b), VÌ SAO CẦN PHI TUYẾN — chứng
      # minh CỤ THỂ bằng số: một neuron tuyến tính KHÔNG THỂ tách XOR (thử
      # mọi tổ hợp trọng số hợp lý, luôn sai ít nhất 1 điểm), MLP forward
      # pass BẰNG TAY (2 tầng, trọng số cho SẴN, chỉ ra tầng ẩn tạo ra một
      # không gian biểu diễn MỚI nơi XOR trở thành tách được tuyến tính —
      # nối tiếp trực tiếp bài trước), khởi tạo trọng số (vì sao khởi tạo 0
      # làm mọi neuron học GIỐNG HỆT nhau — đối xứng không phá được; vì sao
      # khởi tạo quá lớn làm sigmoid/tanh bão hoà — đo THẬT độ lớn gradient
      # cả hai ca; heuristic Xavier/He), độ sâu/độ rộng (đánh đổi năng lực
      # mô hình — nối lại bias-variance của T8.1c), MLP cho hồi quy (không
      # activation ở tầng ra) ĐỐI CHIẾU MLP cho phân loại (sigmoid/softmax ở
      # tầng ra). BOSS quý: ráp class Layer+MLP forward-pass ĐẦY ĐỦ với
      # trọng số cho sẵn, chạy đúng cả XOR (phân loại) lẫn một tập hồi quy
      # nhỏ, đối chiếu byte-for-byte với số tính tay — CHƯA có huấn luyện,
      # đó LÀ việc của q8.2b+q8.2c.
      - dao-ham-tu-dong
      # q8.2b (9 bài, LÕI KỸ THUẬT của T8.2) — đồ thị tính toán (biểu diễn
      # một biểu thức nhiều bước thành DAG các phép toán, mỗi node giữ giá
      # trị+phép toán+node cha), đạo hàm chuỗi ÔN LẠI bằng tay trên một biểu
      # thức vô hướng nhiều bước cụ thể (chỉ ra vì sao làm tay không scale),
      # class Value cộng/trừ/nhân (mỗi phép toán TỰ lưu một closure
      # "backward cục bộ" — công thức undo phép đó), class Value mũ/chia/
      # phi tuyến (thêm pow, tanh, relu — MỖI cái với đúng công thức đạo hàm
      # riêng), sắp xếp tô-pô + duyệt ngược (thuật toán THẬT của `.backward()`
      # — sắp mọi node theo tô-pô rồi lan truyền gradient từ output ngược về
      # từng lá), TÍCH LUỸ gradient (gotcha KINH ĐIỂN của autograd: một Value
      # dùng LẶP LẠI ở nhiều chỗ trong đồ thị PHẢI cộng dồn (+=) gradient chứ
      # không ghi đè (=) — demo SỐ THẬT bản SAI cho gradient sai trên biến
      # dùng hai lần, rồi bản ĐÚNG), kiểm đạo hàm bằng số (finite difference
      # (f(x+h)-f(x-h))/2h — công cụ soát lỗi chuẩn, đối chiếu với gradient
      # giải tích của autograd trên một biểu thức không tầm thường), ĐỐI
      # CHIẾU hồi quy tuyến tính (lấy lại MSE loss của q8.1a, tính gradient
      # bằng autograd THAY VÌ công thức đóng, đối chiếu khớp — bằng chứng cụ
      # thể autograd ĐÚNG trên một bài toán đã biết đáp số). BOSS quý: động
      # cơ Value autograd ĐẦY ĐỦ (~150-200 dòng, đúng ràng buộc MASTERPLAN)
      # vượt qua gradient-check trên nhiều biểu thức phức hợp khác nhau —
      # đây LÀ động cơ mọi quest sau của T8.2 tái dùng.
      - huan-luyen-mang-no-ron
      # q8.2c (9 bài) — neuron/MLP chạy TRÊN autograd (ráp lại class Layer/
      # MLP của q8.2a nhưng giờ forward pass TỰ xây đồ thị tính toán qua
      # Value của q8.2b — nối hai quý trước lại), hàm mất mát mạng nơ-ron
      # (MSE cho hồi quy, softmax+cross-entropy cho phân loại — TÁI DÙNG
      # chiêu log-sum-exp ổn định số học của T8.1c, giờ lan gradient QUA
      # autograd thay vì đạo hàm tay), vòng lặp huấn luyện đầy đủ (forward →
      # loss → backward() → cập nhật tham số → zero_grad, số liệu loss giảm
      # THẬT qua từng epoch trên tập đồ chơi nhỏ), mini-batch SGD (batch vs
      # full-batch vs single-sample — ảnh hưởng độ mượt/tốc độ đường loss),
      # động lượng/momentum (bộ tích luỹ vận tốc — đo THẬT hội tụ nhanh hơn
      # SGD trơn trên CÙNG bài toán), Adam (learning rate thích nghi theo
      # từng tham số qua trung bình động gradient+bình phương gradient — so
      # sánh SỐ THẬT SGD/momentum/Adam: bao nhiêu epoch để chạm cùng một
      # ngưỡng loss), overfitting mạng nơ-ron (quá nhiều neuron ẩn/quá nhiều
      # epoch nhớ vẹt tập nhỏ — đường val loss tách khỏi train loss, nối lại
      # T8.1a), chính quy hoá mạng nơ-ron (L2 weight decay + dropout — ý
      # tưởng dropout ĐƠN GIẢN: ngẫu nhiên triệt tiêu một phần activation lúc
      # huấn luyện, đo THẬT hiệu quả trên val loss). BOSS quý: vòng lặp huấn
      # luyện ĐẦY ĐỦ (Neuron/Layer/MLP + autograd + Adam + regularization)
      # chạy hết trên MỘT tập hồi quy VÀ một tập phân loại đồ chơi, báo cáo
      # loss/accuracy cuối.
      - boss-mang-no-ron-tu-so-0
      # q8.2d (6 bài, BOSS ĐÓNG T8.2) — dữ liệu phi tuyến THẬT (tự dựng tập
      # 2D không tách được tuyến tính — hai vòng tròn đồng tâm hoặc hai hình
      # trăng lồng nhau bằng công thức tường minh, KHÔNG sklearn — chứng
      # minh SỐ THẬT logistic regression của T8.1b THẤT BẠI trên nó, lý do
      # CẦN mạng nơ-ron của track này), ráp framework mạng nơ-ron (nối Value
      # autograd của q8.2b + Neuron/Layer/MLP của q8.2a/q8.2c + Adam của
      # q8.2c thành MỘT khối thống nhất dùng cho phần còn lại của BOSS),
      # huấn luyện trên dữ liệu phi tuyến (MLP vừa ráp phân loại ĐÚNG tập ở
      # bài 1 — số accuracy THẬT trước/sau so với mô hình tuyến tính thất
      # bại), so sánh tối ưu hoá (SGD/momentum/Adam trên CÙNG bài toán phi
      # tuyến này — số epoch THẬT để chạm cùng ngưỡng accuracy, không phải
      # chỉ nói suông "Adam nhanh hơn"), gradient biến mất THẬT (dựng mạng
      # SÂU hơn (nhiều tầng) dùng sigmoid/tanh, ĐO THẬT độ lớn gradient ở
      # tầng đầu co lại RẤT MẠNH so với mạng nông — bắc cầu TỚI T8.3, nơi
      # kiến trúc tốt hơn sẽ giải quyết vấn đề này). BOSS ĐÓNG T8.2 (32/32):
      # ráp CUỐI autograd + framework MLP + optimizer + regularization,
      # huấn luyện đầu-cuối trên dữ liệu phi tuyến, bàn giao ĐÚNG động cơ
      # autograd này cho T8.3 (Transformer từ số 0 sẽ mở rộng autograd lên
      # tensor/attention, KHÔNG viết lại từ đầu).
      #
      # T8.3 "Transformer từ số 0" (40 bài, 5 quest — MASTERPLAN chỉ cho một
      # dòng mô tả "BPE tay → attention tính tay trên 4 token → micro-
      # transformer 2 tầng numpy, corpus 5KB, chạy <2s [+ lab TUỲ CHỌN GPT-2
      # đầy đủ, notebook, KHÔNG chấm]" — TỰ thiết kế breakdown cấp quest,
      # tham khảo bản đồ khái niệm ở `references/repos/ai-for-beginners/
      # lessons/5-NLP/18-Transformers/` (attention/positional encoding/multi-
      # head, encoder-decoder) và `references/articles/www_k-a_in_pyt-
      # miniGPT.qmd` (kiến trúc GPT tối giản: embed+pos → khối transformer →
      # tầng tuyến tính cuối) — CHỈ lấy trình tự khái niệm, không sao chép
      # văn xuôi/code. QUYẾT ĐỊNH THIẾT KẾ: mở rộng `Value` (vô hướng, T8.2)
      # thành `Tensor` (mảng numpy) thay vì viết công thức đóng riêng cho
      # từng phép toán — giữ ĐÚNG triết lý "một động cơ, tái dùng khắp track"
      # đã chứng minh hiệu quả suốt T8.2, và tránh vòng lặp attention từng vô
      # hướng (quá chậm cho corpus thật). Mọi phép toán MỚI của `Tensor`
      # (matmul, softmax, layer-norm) đều PHẢI kiểm bằng finite-difference
      # (đúng kỹ thuật `kiem-dao-ham-bang-so`, q8.2b) trước khi tin, không
      # suy luận công thức đạo hàm ma trận bằng tay mà không verify.
      - token-hoa-bpe
      # q8.3a (8 bài) — vì sao cần token hoá (đánh đổi từ-nguyên-vẹn vs
      # ký-tự-đơn, vấn đề từ ngoài từ vựng), token hoá theo ký tự (baseline
      # đơn giản nhất, tự viết), đếm cặp ký tự liền kề (bước lõi của BPE —
      # tần suất mọi cặp liền kề trong corpus), gộp cặp phổ biến nhất (một
      # vòng merge BPE — thay MỌI lần xuất hiện cặp đó bằng một token mới),
      # lặp merge tới kích thước từ vựng mục tiêu (vòng lặp BPE đầy đủ), mã
      # hoá văn bản MỚI (áp danh sách merge đã học lên câu chưa từng thấy
      # lúc huấn luyện BPE), giải mã ngược (token ID → văn bản). BOSS quý:
      # BPE đầy đủ trên một corpus nhỏ tự nghĩ, tokenize rồi detokenize
      # round-trip đúng nguyên văn.
      - dong-co-tensor
      # q8.3b (8 bài) — vì sao cần Tensor (không phải Value vô hướng —
      # attention cần nhân MA TRẬN, lặp từng phần tử một quá chậm cho corpus
      # thật), class Tensor cơ bản (bọc mảng numpy, `+`/`-` element-wise,
      # TÁI DÙNG ý tưởng `_prev`/`_backward` của `Value`), nhân ma trận và
      # đạo hàm của nó (matmul forward+backward, công thức đạo hàm ma trận,
      # KIỂM bằng finite-difference trước khi tin), chuyển vị/định hình lại
      # (transpose/reshape — cần để tách nhiều "đầu" attention sau này),
      # softmax qua Tensor (tái dùng log-sum-exp ổn định số học của T8.1c,
      # giờ áp trên cả một mảng), kiểm đạo hàm ma trận bằng số (mở rộng kỹ
      # thuật của q8.2b sang Tensor, xác nhận matmul backward đúng), layer
      # normalization (chuẩn hoá theo mỗi token — công thức+đạo hàm, kỹ
      # thuật MỚI mà T8.2 chưa cần). BOSS quý: động cơ Tensor đầy đủ
      # (matmul+softmax+layer-norm) qua gradient-check trên biểu thức phức
      # hợp.
      - co-che-attention
      # q8.3c (9 bài) — embedding token (bảng tra cứu, mỗi token ID → một
      # vector, MA TRẬN tham số học được), vị trí trong chuỗi (positional
      # encoding — vì sao attention một mình không biết thứ tự, cộng thêm
      # vector vị trí), Query/Key/Value (ba phép chiếu tuyến tính từ CÙNG
      # một embedding), điểm attention (Q·Kᵀ — đo độ liên quan giữa MỌI cặp
      # vị trí, TÍNH TAY trên đúng 4 token cụ thể — đúng yêu cầu MASTERPLAN),
      # chia tỉ lệ+softmax (chia `√d_k` rồi softmax theo hàng, tổng mỗi hàng
      # =1), tổng trọng số của Value (nhân trọng số attention với V — đầu ra
      # MỘT đầu attention, TÍNH TAY tiếp trên 4 token), che tương lai
      # (causal mask — vì sao mô hình sinh văn bản không được nhìn thấy
      # token tương lai, che bằng `-inf` trước softmax), đa đầu attention
      # (multi-head — nhiều attention song song trên không gian con khác
      # nhau, ghép lại). BOSS quý: một tầng attention đầy đủ (multi-head +
      # causal mask) chạy trên 4 token, đối chiếu byte-for-byte với số tính
      # tay ở hai bài trước.
      - khoi-transformer-va-huan-luyen
      # q8.3d (9 bài) — kết nối tắt (residual connection — cộng đầu vào gốc
      # vào đầu ra tầng con, bắc cầu từ vanishing gradient của T8.2), mạng
      # truyền thẳng theo vị trí (feedforward hai tầng, áp ĐỘC LẬP cho mỗi
      # vị trí), ráp MỘT khối Transformer (attention+residual+layer-norm+
      # feedforward+residual+layer-norm, đúng thứ tự), xếp chồng hai khối
      # (đúng "2 tầng" MASTERPLAN yêu cầu), từ Tensor cuối tới xác suất từ
      # tiếp theo (tầng tuyến tính cuối + softmax trên từ vựng), hàm mất mát
      # dự đoán từ tiếp theo (cross-entropy, tái dùng log-sum-exp), vòng lặp
      # huấn luyện trên corpus nhỏ (forward→loss→backward→Adam, tái dùng
      # `adam_step_l2` của T8.2, trên corpus THẬT ~5KB), sinh văn bản
      # (autoregressive — dự đoán từng token, nối vào chuỗi, lặp lại). BOSS
      # quý: huấn luyện transformer 2 tầng trên corpus nhỏ, đo loss giảm
      # THẬT, sinh một đoạn văn bản mẫu.
      - boss-transformer-tu-so-0
      # q8.3e (6 bài, BOSS ĐÓNG T8.3) — vì sao attention không bị vanishing
      # gradient (đối chiếu TRỰC TIẾP với `gradient-bien-mat-that` của
      # T8.2 — đường tắt trực tiếp giữa MỌI cặp vị trí, không phải chuỗi
      # tầng dài), so sánh MLP/kiến-trúc-cũ với Transformer trên một bài
      # toán phụ thuộc XA (đo THẬT accuracy/loss), ráp pipeline đầy đủ (BPE
      # + embedding + Transformer 2 tầng, từ văn bản thô tới sinh văn bản),
      # huấn luyện trên corpus ~5KB THẬT + đo thời gian chạy (mục tiêu <2s
      # theo MASTERPLAN), giới hạn của micro-transformer này (kích thước
      # nhỏ, dễ overfit trên corpus nhỏ — nối lại bias-variance, KHÔNG phải
      # GPT thật, chỉ mô hình MINH HOẠ nguyên lý). BOSS ĐÓNG T8.3 (40/40):
      # tổng kết toàn track, bàn giao sang T8.4 "Dùng LLM đúng cách" (từ XÂY
      # DỰNG mô hình sang SỬ DỤNG mô hình đã có sẵn).
      #
      # T8.4 "Dùng LLM đúng cách" (28 bài, 5 quest — MASTERPLAN chỉ cho một
      # dòng mô tả "Dùng LLM đúng cách" — TỰ thiết kế breakdown cấp quest,
      # tham khảo references/articles/www_k-a_in_cot.qmd (chain-of-thought —
      # chỉ trình tự khái niệm, không sao chép văn xuôi/code). QUYẾT ĐỊNH
      # THIẾT KẾ bắt buộc (ràng buộc nền tảng của CẢ track): toàn bộ nền
      # tảng này chạy Pyodide client-side, KHÔNG có mạng — không lời giải
      # nào có thể gọi một LLM THẬT qua API, và mọi phép chấm điểm phải TẤT
      # ĐỊNH (cùng input luôn cho cùng output, verify được qua kiemAst thật
      # + so output). Giải quyết bằng CÁCH TÁCH: (a) các bài về CHIẾN LƯỢC
      # GIẢI MÃ (quest 2) dùng THẬT `Tensor`/`khoi_transformer` đã xây ở
      # T8.3 — phân phối xác suất đầu ra CÓ THẬT, thuật toán sampling áp lên
      # đó là thứ được kiểm; (b) mọi bài còn lại (vai trò message, few-shot,
      # function-calling, đánh giá, an toàn) dùng một "LLM MÔ PHỎNG" — một
      # hàm Python THUẦN nhận vào prompt/message, tra theo BẢNG hoặc LUẬT cố
      # định đã biết trước (không phải mạng nơ-ron nào), đóng vai trò "phản
      # hồi API" để bài học có thứ CỤ THỂ mà tất định để phân tích/parse/
      # validate — giữ đúng tinh thần "đo bằng số thật, không suy luận suông"
      # của toàn khoá, chỉ khác đối tượng đo là HÀNH VI PARSE/CHIẾN LƯỢC,
      # không phải "độ thông minh" của một mô hình giả lập.
      - prompt-engineering-nen-tang
      # q8.4a (6 bài) — vai trò system/user/assistant trong một cuộc gọi
      # (cấu trúc message, không phải văn xuôi liền mạch), zero-shot vs
      # few-shot (đo được bằng số qua LLM mô phỏng — bao nhiêu ví dụ mẫu
      # đổi hành vi ra sao), ràng buộc rõ ràng chống mơ hồ (một chỉ thị mơ
      # hồ cho ra nhiều cách hiểu, một chỉ thị rõ ràng thu hẹp về một),
      # prompt template có tham số (chuỗi khuôn mẫu điền biến, tái dùng
      # được), đo thực nghiệm A/B ảnh hưởng của prompt (so 2 phiên bản
      # prompt trên CÙNG LLM mô phỏng, đo chênh lệch bằng số), prompt suy
      # luận từng bước (chain-of-thought — vì sao yêu cầu "suy nghĩ từng
      # bước" đổi được đầu ra trên bài toán nhiều bước). BOSS quý: ráp một
      # prompt hoàn chỉnh (vai trò+ràng buộc+few-shot+CoT) đo cải thiện so
      # với prompt trần trụi.
      - chien-luoc-giai-ma-va-lay-mau
      # q8.4b (6 bài) — ôn greedy decoding (T8.3, lấy xác suất cao nhất mỗi
      # bước) và giới hạn của nó (luôn cho CÙNG một đầu ra, không đa dạng),
      # lấy mẫu ngẫu nhiên từ phân phối softmax THẬT (dùng đúng
      # `khoi_transformer`/logits của T8.3, không mô phỏng), nhiệt độ
      # (temperature — chia logit trước softmax, đo entropy phân phối thay
      # đổi ra sao khi nhiệt độ tăng/giảm), top-k sampling (chỉ giữ k xác
      # suất cao nhất, chuẩn hoá lại rồi lấy mẫu), top-p/nucleus sampling
      # (giữ tập nhỏ nhất có tổng xác suất ≥p, số lượng token động theo
      # từng bước — khác top-k cố định). BOSS quý: so 4 chiến lược (greedy,
      # temperature, top-k, top-p) TRÊN CÙNG một mô hình thật đã huấn
      # luyện ở T8.3, đo đa dạng đầu ra bằng số (ví dụ: số văn bản khác
      # nhau sinh ra từ nhiều lần chạy).
      - dau-ra-co-cau-truc-va-cong-cu
      # q8.4c (5 bài) — vì sao cần đầu ra có cấu trúc (JSON máy phân tích
      # được, không phải văn xuôi tự do máy phải đoán), ràng buộc theo
      # schema + retry khi sai (kiểm đầu ra LLM mô phỏng khớp schema định
      # trước chưa, không khớp thì yêu cầu lại), function-calling mô phỏng
      # (LLM mô phỏng trả về tên hàm+tham số dạng có cấu trúc thay vì gọi
      # thật), vòng lặp ReAct (suy luận→gọi công cụ mô phỏng→nhận kết
      # quả→lặp tới khi đủ thông tin trả lời). BOSS quý nhỏ: một "trợ lý"
      # nhận câu hỏi, gọi ĐÚNG công cụ mô phỏng cần thiết, trả JSON đúng
      # schema.
      - quan-ly-ngu-canh-va-hoi-thoai
      # q8.4d (5 bài) — đếm token của một đoạn văn bản (tái dùng BPE THẬT
      # của q8.3a, không đếm ký tự/từ), cửa sổ ngữ cảnh có trần (context
      # window — mọi LLM có giới hạn số token đầu vào), cắt bớt lịch sử khi
      # vượt trần (chiến lược giữ gần nhất/giữ đầu+cuối, đo bằng số token
      # trước/sau), tóm tắt lịch sử hội thoại cũ (nén nhiều lượt cũ thành
      # một đoạn ngắn mô phỏng, giữ ngữ nghĩa cốt lõi), bộ nhớ hội thoại
      # nhiều lượt (giữ trạng thái xuyên nhiều lượt hỏi-đáp). BOSS quý nhỏ:
      # vòng lặp hội thoại nhiều lượt tự cắt/tóm tắt khi gần đầy cửa sổ
      # ngữ cảnh, đo token trước/sau mỗi lượt.
      - boss-dung-llm-dung-cach
      # q8.4e (6 bài, BOSS ĐÓNG T8.4) — ảo giác/hallucination (đo được:
      # câu trả lời có nằm ngoài phạm vi ngữ cảnh cho phép không — so
      # sánh với văn bản nguồn), đánh giá tự động đầu ra LLM (exact-match/
      # độ tương đồng tự cài, không dùng thư viện ngoài), LLM-làm-giám-
      # khảo (khái niệm dùng một LLM mô phỏng thứ hai chấm đầu ra LLM mô
      # phỏng thứ nhất theo luật cố định), prompt injection/jailbreak
      # (nhận diện chỉ thị độc hại giấu trong dữ liệu đầu vào — dữ liệu
      # không phải lệnh), rủi ro PII/nội dung nhạy cảm (phát hiện mẫu
      # thông tin cá nhân trong văn bản trước khi gửi đi). BOSS: ráp một
      # trợ lý nhỏ dùng ĐÚNG mọi kỹ thuật cả track (prompt có cấu trúc,
      # chiến lược giải mã, đầu ra JSON+công cụ, quản lý ngữ cảnh, kiểm ảo
      # giác/injection), đóng T8.4 tại 28/28, bàn giao sang T8.5 "RAG từ
      # số 0".
      #
      # T8.5 "RAG từ số 0" (34 bài, 6 quest — MASTERPLAN chỉ cho một dòng
      # mô tả "RAG từ số 0 (34, HNSW tự cài, BM25 hybrid)" — TỰ thiết kế
      # breakdown cấp quest, không tìm thấy tài liệu tham khảo chuyên sâu
      # về RAG/HNSW/BM25 trong references/ (chỉ có một ảnh minh hoạ
      # "words to vectors" ở references/repos/ai-for-beginners — không đủ
      # chi tiết để trích dẫn kỹ thuật, chỉ dùng làm gợi ý trực giác).
      # QUYẾT ĐỊNH THIẾT KẾ bắt buộc: vì Pyodide KHÔNG có mạng nên KHÔNG
      # thể tải một mô hình embedding THẬT đã huấn luyện trước — "embedding"
      # trong track này là một VECTOR TỰ CHẾ, tất định, tự tính được ngay
      # trong sandbox (ví dụ vector đếm từ có trọng số/TF trên một từ vựng
      # cố định nhỏ, hoặc — khi cần vector "học" thật sự — tái dùng NGUYÊN
      # VẸN bảng embedding token đã huấn luyện ở T8.3, gộp trung bình theo
      # BPE token của một đoạn văn bản) — KHÔNG gọi API embedding thật, chỉ
      # dạy CƠ CHẾ (đo tương đồng, đánh chỉ mục, tìm kiếm) trên vector CÓ
      # THẬT do người học tự tính, không phải chất lượng ngữ nghĩa của một
      # mô hình embedding sản xuất thật.
      - chunking-va-embedding-tu-che
      # q8.5a (6 bài) — vì sao cần RAG (kiến thức một LLM đóng băng ở thời
      # điểm huấn luyện, cửa sổ ngữ cảnh có trần — bài học q8.4d — không
      # thể nhét MỌI tài liệu vào một lượt gọi), chia văn bản dài thành
      # đoạn nhỏ (chunking — theo câu, theo cửa sổ trượt CÓ chồng lấn để
      # không cắt đứt ý giữa hai đoạn), xây một vector đại diện đoạn văn
      # tự chế (đếm từ có trọng số trên từ vựng cố định — "embedding" tự
      # chế, KHÔNG phải mô hình thật), đo hai đoạn GẦN NGHĨA cho vector
      # GẦN NHAU hơn hai đoạn không liên quan (đo bằng khoảng cách/tương
      # đồng thật, không suy đoán). BOSS quý: chia + tính vector cho một
      # tài liệu nhỏ tự nghĩ, đối chiếu các cặp đoạn gần/xa nghĩa.
      - tim-kiem-vector-va-do-tuong-dong
      # q8.5b (5 bài) — tích vô hướng (dot product) đo mức độ "cùng
      # hướng" của hai vector, độ tương đồng cosin (chuẩn hoá độ dài,
      # tách bạch "cùng hướng" khỏi "cùng độ lớn"), tìm k lân cận gần
      # nhất bằng VÉT CẠN (brute-force k-NN — so đúng MỌI cặp, chậm nhưng
      # luôn đúng, làm chuẩn đối chiếu cho quest sau). BOSS quý: tìm đúng
      # k đoạn văn liên quan nhất cho một câu hỏi, trên tập đoạn văn của
      # bài trước.
      - hnsw-tu-cai
      # q8.5c (8 bài) — ý tưởng đồ thị điều hướng nhỏ (navigable small
      # world — mỗi điểm nối tới vài "hàng xóm gần", đi men theo cạnh để
      # tới đích nhanh hơn quét toàn bộ), cấu trúc NHIỀU TẦNG của HNSW
      # (tầng trên thưa để nhảy xa, tầng dưới dày để tinh chỉnh), thuật
      # toán CHÈN một điểm mới vào đồ thị (chọn tầng ngẫu nhiên CÓ SEED
      # cố định để tất định, nối cạnh theo đúng luật), thuật toán TÌM
      # KIẾM (đi tham lam từ tầng trên xuống tầng dưới), đo ĐỘ CHÍNH XÁC
      # (recall so với vét cạn q8.5b — bao nhiêu kết quả HNSW trả về THẬT
      # SỰ nằm trong top-k đúng) và tốc độ (đo số bước so sánh, không
      # phải luôn quét hết). BOSS quý (đóng phần lõi kỹ thuật khó nhất
      # track): HNSW tự cài đạt recall cao, số phép so sánh ÍT HƠN HẲN
      # vét cạn trên một tập đoạn văn đủ lớn.
      - bm25-va-tim-kiem-tu-khoa
      # q8.5d (6 bài) — vì sao cần tìm kiếm theo TỪ KHOÁ bên cạnh vector
      # (một tên riêng/mã số hiếm gặp có thể không được vector "học" tốt,
      # nhưng khớp từ khoá CHÍNH XÁC luôn bắt được), tần suất từ trong
      # một đoạn (term frequency), độ hiếm của từ trên toàn kho ngữ liệu
      # (inverse document frequency — một từ xuất hiện trong CÀNG ÍT đoạn
      # thì càng có giá trị phân biệt), công thức BM25 đầy đủ (kết hợp
      # TF+IDF với hai tham số điều chỉnh k1/b, chống việc một từ lặp quá
      # nhiều lấn át điểm số), xếp hạng mọi đoạn văn theo điểm BM25 cho
      # một câu hỏi. BOSS quý: BM25 tự cài xếp đúng thứ hạng trên một bộ
      # câu hỏi đối chứng đã biết trước đáp án đúng.
      - hybrid-va-rerank
      # q8.5e (4 bài) — kết hợp kết quả tìm kiếm VECTOR (q8.5b/c) và TỪ
      # KHOÁ (BM25, q8.5d) — "BM25 hybrid" theo đúng yêu cầu MASTERPLAN —
      # qua một công thức hợp nhất thứ hạng cố định (reciprocal rank
      # fusion — cộng nghịch đảo thứ hạng từ cả hai danh sách, không cần
      # chuẩn hoá điểm số khác thang đo), đo hybrid tìm ĐÚNG nhiều đoạn
      # liên quan hơn TỪNG phương pháp đứng riêng trên một câu hỏi mà
      # vector yếu (từ hiếm) HOẶC BM25 yếu (diễn đạt khác từ). BOSS quý
      # nhỏ: hybrid vượt cả hai phương pháp riêng lẻ, đo bằng số cụ thể.
      - boss-rag-tu-so-0
      # q8.5f (5 bài, BOSS ĐÓNG T8.5 TẠI 34/34) — ráp TOÀN BỘ pipeline:
      # chunking+vector tự chế (q8.5a) → chỉ mục HNSW (q8.5c) → tìm kiếm
      # hybrid BM25+vector (q8.5e) → GHÉP các đoạn tìm được vào một prompt
      # có cấu trúc (tái dùng nguyên tắc q8.4a/c) → LLM mô phỏng trả lời
      # DỰA TRÊN ngữ cảnh vừa truy xuất, không phải "kiến thức nội tại".
      # Đo cải thiện bằng số: LLM mô phỏng CÓ RAG (đọc đúng đoạn liên
      # quan trước khi trả lời) so với KHÔNG có RAG (phải "đoán" trên một
      # câu hỏi ngoài phạm vi kiến thức có sẵn của nó — tái hiện đúng
      # phép đo ảo giác của q8.4e). Đóng T8.5 tại 34/34, bàn giao sang
      # T8.6 "Hạ tầng & vận hành AI" (26 bài, TÁI DÙNG NGUYÊN theo
      # MASTERPLAN — không xây thuật toán mới, ráp lại hạ tầng đã có).
