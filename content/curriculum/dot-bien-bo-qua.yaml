# Đột biến được tha, kèm lý do
#
# `tools/kiem_dot_bien.mjs` sửa một Ý trong phần người học phải điền rồi hỏi
# cách chấm có bắt được không. Bắt không được thì hoặc cách chấm hổng, hoặc
# đột biến ấy vô hại. Tệp này khai loại thứ hai — TỪNG CÁI MỘT, kèm lý do.
#
# Khai bừa vào đây để cổng xanh là tự tay dựng lại đúng thứ cả dự án đi bắt:
# một con số xanh không đo thứ nó nói là nó đo. Mỗi dòng dưới đây phải trả lời
# được câu "vì sao đáp án ấy KHÔNG phải là đáp án sai".
#
# Một điều rút ra từ vòng phân loại đầu tiên, đáng ghi lại: phần lớn ngưỡng ở
# Realm 0 và Realm 1 không kiểm được chỗ hoà, vì bài chấm mã viết thẳng trên
# ĐÚNG MỘT bộ dữ liệu cố định. Muốn kiểm chỗ hoà thì hoặc đổi dữ liệu (kéo
# theo mọi bài dùng chung sổ ấy), hoặc gói việc vào một hàm rồi gọi nhiều lần
# — mà hàm là khái niệm của T1.3, tới sau. Từ T1.3 trở đi thì không còn cớ ấy.

# DỮ LIỆU KHÔNG CHẠM MỐC. `so_km <= 0` là chốt chặn dữ liệu vô lý; không đơn nào đúng 1 km. Khái niệm của bài là cổng chặn sớm, không phải chỗ hoà.
- 'nen-tang.ham-vien-gach.chan-ngay-o-cua · cong-cho-don-giao-hang · đổi MỌI hằng số 0 thành 1 (1 chỗ)'

# BÀI BOSS, KHÔNG KHÁI NIỆM MỚI. Ngưỡng 100 000 chỉ là một tham số của đề; không khoản nào đúng 100 000. Bài chấm sáu chặng khác nhau, mỗi chặng một công cụ của cả track.
- 'nen-tang.list-dict-set-tuple.boss-so-chi-tieu-thang · ghep-ca-cuon-so-thang · đổi MỌI hằng số 100000 thành 100001 (1 chỗ)'

# DỮ LIỆU KHÔNG CÓ CẶP BẰNG NHAU. `<` và `<=` trên tuple chỉ khác nhau khi hai tuple bằng hệt nhau; ba cặp của bài đều khác nhau. Bài dạy thứ tự so sánh ô-đầu-trước, không dạy chỗ hoà.
- 'nen-tang.list-dict-set-tuple.may-nhin-o-dau-truoc · hoi-thang-may-cap-nao-truoc · đổi MỌI dấu < thành <= (2 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI — bài dạy thứ tự tính `and`/`or`/`not`. Xem lý do ở `neu-thi`.
- 'nen-tang.re-nhanh-va-lap.ai-duoc-tinh-truoc · mot-dong-bon-bac · đổi MỌI dấu > thành >= (4 chỗ)'

# TƯƠNG ĐƯƠNG THẬT trên con số kỷ lục. Chỉ khác nếu có hai ngày BẰNG NHAU cùng giữ kỷ lục (khi ấy `>=` giữ ngày sau) — sổ này không có ngày trùng tiền.
- 'nen-tang.re-nhanh-va-lap.boss-so-chi-tieu-cua-byte · giu-lai-ngay-ky-luc · đổi MỌI dấu > thành >= (2 chỗ)'

# TƯƠNG ĐƯƠNG THẬT. `range(30, 120, 20)` và `range(30, 121, 20)` cùng cho 30, 50, 70, 90, 110 — bước 20 không bao giờ đặt chân vào khoảng 120…121.
- 'nen-tang.re-nhanh-va-lap.buoc-nhay-cua-range · hot-bot-noi-nuoc-dung · đổi MỌI hằng số 120 thành 121 (1 chỗ)'

# SỔ DÙNG CHUNG BỐN BÀI. Sổ `[45000, 120000, 30000, 260000, 85000, 210000]` xuất hiện 17 chỗ trong bốn bài 12–15; đổi một số để chạm mốc 200 000 sẽ làm trôi tổng của bài 12 và 13 — đúng cái mà cổng sổ sự thật dựng ra để chặn. Khái niệm của bài là ĐẾM LƯỢT, không phải chỗ hoà của ngưỡng.
- 'nen-tang.re-nhanh-va-lap.dem-nhung-luot-dang-ke · dem-ngay-vuot-nguong · đổi MỌI dấu > thành >= (1 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI — bài dạy `if` lồng trong `if`. Xem lý do ở `neu-thi`.
- 'nen-tang.re-nhanh-va-lap.dieu-kien-trong-dieu-kien · them-mot-tang · đổi MỌI dấu > thành >= (2 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI — bài dạy gộp `if` lồng thành một dòng `and`. Xem lý do ở `neu-thi`.
# Số chỗ đi từ 1 lên 4 vì bài bỏ cái hàm ba tham số (chưa bài nào dạy) và trải thành bốn cảnh gán thẳng.
- 'nen-tang.re-nhanh-va-lap.duong-di-toi-mot-dong · viet-lai-cho-phang · đổi MỌI dấu > thành >= (4 chỗ)'

# TƯƠNG ĐƯƠNG THẬT. `>` và `>=` trong một vòng tìm max cho ra ĐÚNG cùng một con số, mọi dữ liệu. Không cách chấm nào phân biệt nổi, vì không có gì để phân biệt.
- 'nen-tang.re-nhanh-va-lap.giu-lai-ky-luc · tim-ngay-cao-nhat · đổi MỌI dấu > thành >= (1 chỗ)'

# SỔ DÙNG CHUNG — xem lý do ở `dem-nhung-luot-dang-ke`. Khái niệm của bài là thứ tự nhánh `elif`: nhánh sau không bao giờ tới lượt nếu nhánh trước đã trùm.
- 'nen-tang.re-nhanh-va-lap.nhanh-khong-bao-gio-toi · xep-lai-hang-ro · đổi MỌI dấu > thành >= (4 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI — bài dạy `or`. Xem lý do ở `neu-thi`.
- 'onboarding.ra-lenh-cho-byte.chi-can-mot-ve-dung · tam-bien-khuyen-mai · đổi MỌI dấu > thành >= (1 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI — bài dạy `and`. Xem lý do ở `neu-thi`.
- 'onboarding.ra-lenh-cho-byte.hai-dieu-kien-cung-dung · dan-to-giay-len-tuong · đổi MỌI dấu < thành <= (1 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI. Bài dạy `if` — đúng một khái niệm. Thêm một ca đúng 65 tuổi để phân biệt `>` với `>=` là lén đưa vào khái niệm thứ hai, phạm luật "mỗi bài đúng một khái niệm mới". Chỗ khác nhau giữa hai dấu ấy do bài `dung-hay-sai` dạy, và bài ấy CÓ chấm đúng cái mốc.
- 'onboarding.ra-lenh-cho-byte.neu-thi · dat-mien-phi · đổi MỌI dấu > thành >= (2 chỗ)'

# HẰNG SỐ MINH HOẠ. Bài chứng minh phép trừ không giao hoán; `12 − 5 == 5 − 12` và `13 − 5 == 5 − 13` đều `False`, và mọi cặp số khác nhau đều minh hoạ đúng cái luật ấy. Con số cụ thể không phải thứ bài dạy.
- 'toan.cam-nhan-so.mang-chu-nhat-xoay-mot-goc · trong-tai-hai-cau · đổi MỌI hằng số 12 thành 13 (2 chỗ)'

# HẰNG SỐ MINH HOẠ. Bài dạy "phân số vượt được số 1"; `7/4 > 1` hay `8/4 > 1` đều `True` và đều minh hoạ đúng điều đó.
- 'toan.cam-nhan-so.phan-so-khong-bi-nhot-duoi-mot · do-hai-luong · đổi MỌI hằng số 7 thành 8 (1 chỗ)'

# HẰNG SỐ MINH HOẠ. `3/4 == 3/5` và `3/4 == 3/6` đều `False`; bài dạy "mẫu khác thì không bằng nhau", đúng với mọi mẫu khác.
- 'toan.cam-nhan-so.phan-so-la-may-lan-don-vi-moi · hoi-may-hai-cau · đổi MỌI hằng số 5 thành 6 (1 chỗ)'

# HẰNG SỐ MINH HOẠ. Bài dạy dấu `=` là một CÂU HỎI; lời khẳng định sai vẫn sai dù hỏi 21 ổ hay 22 ổ.
- 'toan.dai-so-va-ham-so.dau-bang-la-mot-cau-hoi · may-phan-ba-loi-khang-dinh · đổi MỌI hằng số 21 thành 22 (1 chỗ)'

# HẰNG SỐ NGƯỠNG KHÔNG BỊ GHIM. Bài CÓ chấm đúng cái mốc n = 10 (lãi đúng 20 000, `con_hon_10` phải `False`), nên nó phân biệt được `>` với `>=` — thứ đột biến này chạm tới chỉ là con số 20 000 trong ngưỡng, mà dữ liệu không có mức lãi nào rơi vào 20 001.
- 'toan.dai-so-va-ham-so.khi-dau-phai-quay-nguoc · hai-luat-tranh-nhau · đổi MỌI hằng số 20000 thành 20001 (3 chỗ)'


# ── Vòng hai: đột biến HẰNG SỐ NGƯỠNG ──────────────────────────────────
# Tha xong lớp đột biến so sánh thì cổng lộ ra lớp thứ hai ở cùng những bài
# ấy: đổi chính con số ngưỡng đi 1. Cùng một lẽ — bộ dữ liệu cố định của
# bài không có giá trị nào rơi vào khe vừa mở ra, và con số ngưỡng là tham
# số của ĐỀ chứ không phải khái niệm bài dạy.

# Ngưỡng 200 000 là tham số của đề; bài dạy thứ tự tính `and`/`or`/`not`. Sổ dùng chung bốn bài.
- 'nen-tang.re-nhanh-va-lap.ai-duoc-tinh-truoc · mot-dong-bon-bac · đổi MỌI hằng số 200000 thành 200001 (4 chỗ)'

# Ngưỡng 200 000 là tham số của đề; bài dạy ĐẾM LƯỢT. Sổ `[45000…210000]` nằm ở 17 chỗ trong bốn bài 12–15, nên đổi số là làm trôi tổng của bài 12 và 13 — đúng thứ cổng sổ sự thật dựng ra để chặn.
- 'nen-tang.re-nhanh-va-lap.dem-nhung-luot-dang-ke · dem-ngay-vuot-nguong · đổi MỌI hằng số 200000 thành 200001 (1 chỗ)'

# Ngưỡng 500 000 là tham số của đề; bài dạy `if` lồng trong `if`.
- 'nen-tang.re-nhanh-va-lap.dieu-kien-trong-dieu-kien · them-mot-tang · đổi MỌI hằng số 500000 thành 500001 (2 chỗ)'

# Mốc 65 là tham số của đề; bài dạy gộp `if` lồng thành một dòng `and`.
- 'nen-tang.re-nhanh-va-lap.duong-di-toi-mot-dong · viet-lai-cho-phang · đổi MỌI hằng số 65 thành 66 (4 chỗ)'

# Ngưỡng 200 000 / 100 000 là tham số của đề; bài dạy thứ tự nhánh `elif`. Sổ dùng chung bốn bài.
- 'nen-tang.re-nhanh-va-lap.nhanh-khong-bao-gio-toi · xep-lai-hang-ro · đổi MỌI hằng số 200000 thành 200001 (2 chỗ)'

# Ngưỡng 200 000 / 100 000 là tham số của đề; bài dạy thứ tự nhánh `elif`. Sổ dùng chung bốn bài.
- 'nen-tang.re-nhanh-va-lap.nhanh-khong-bao-gio-toi · xep-lai-hang-ro · đổi MỌI hằng số 100000 thành 100001 (2 chỗ)'

# Mốc 65 là tham số của đề; bài dạy `or`.
- 'onboarding.ra-lenh-cho-byte.chi-can-mot-ve-dung · tam-bien-khuyen-mai · đổi MỌI hằng số 65 thành 66 (1 chỗ)'

# Mốc 8 giờ là tham số của đề; bài dạy `and`.
- 'onboarding.ra-lenh-cho-byte.hai-dieu-kien-cung-dung · dan-to-giay-len-tuong · đổi MỌI hằng số 8 thành 9 (1 chỗ)'

# Mốc 65 là tham số của ĐỀ, không phải khái niệm; bài dạy `if`. Khách trong bài 70 và 40 tuổi.
- 'onboarding.ra-lenh-cho-byte.neu-thi · dat-mien-phi · đổi MỌI hằng số 65 thành 66 (2 chỗ)'

# TƯƠNG ĐƯƠNG THẬT, đã tính tay: `12 − 5 == 5 − 12` và `12 − 6 == 6 − 12` đều `False`. Bài chứng minh phép trừ KHÔNG giao hoán, và điều ấy đúng với mọi cặp số khác nhau — con số cụ thể không phải thứ bài dạy.
- 'toan.cam-nhan-so.mang-chu-nhat-xoay-mot-goc · trong-tai-hai-cau · đổi MỌI hằng số 5 thành 6 (2 chỗ)'

# TƯƠNG ĐƯƠNG THẬT, đã tính tay: `7/4 > 1` và `7/5 > 1` đều `True`; `3/4 > 1` và `3/5 > 1` đều `False`; `4/4 > 1` cũng `False`. Bài dạy "phân số vượt được số 1", đúng với mọi mẫu ấy. Đã soi kỹ hơn: bài KHÔNG thiếu chỗ hoà — khối test của nó có sẵn `assert 4/4 == 1`. Và thêm một câu `print(4/4 > 1)` cũng không đóng được đột biến này, vì `5/5 > 1` cũng `False` y như `4/4 > 1`. Nên không sửa: một thay đổi không đóng được gì mà chỉ làm bài dài thêm thì không đáng.
- 'toan.cam-nhan-so.phan-so-khong-bi-nhot-duoi-mot · do-hai-luong · đổi MỌI hằng số 4 thành 5 (2 chỗ)'

# TƯƠNG ĐƯƠNG THẬT, đã tính tay: `7/4 > 1` và `7/5 > 1` đều `True`; `3/4 > 1` và `3/5 > 1` đều `False`; `4/4 > 1` cũng `False`. Bài dạy "phân số vượt được số 1", đúng với mọi mẫu ấy. Đã soi kỹ hơn: bài KHÔNG thiếu chỗ hoà — khối test của nó có sẵn `assert 4/4 == 1`. Và thêm một câu `print(4/4 > 1)` cũng không đóng được đột biến này, vì `5/5 > 1` cũng `False` y như `4/4 > 1`. Nên không sửa: một thay đổi không đóng được gì mà chỉ làm bài dài thêm thì không đáng.
- 'toan.cam-nhan-so.phan-so-khong-bi-nhot-duoi-mot · do-hai-luong · đổi MỌI hằng số 3 thành 4 (1 chỗ)'

# TƯƠNG ĐƯƠNG THEO DỮ LIỆU. Câu hỏi thứ hai là `re_ot > re_hung`, tức `-5 > -2` — `False`. Đổi `re_hung` thành `ngon_cai` cho `-5 > 5` — cũng `False`. Hai câu sai vì hai lẽ khác nhau mà màn hình không phân biệt được. ĐÁNG SỬA SAU: thêm một câu hỏi thứ ba mà đáp án là `True` ở phía số âm (ví dụ `re_hung > re_ot`) thì chỗ này lộ ngay.
- 'toan.cam-nhan-so.lon-hon-la-dung-ben-phai · hai-cau-hoi-cung-mot-kieu · đổi MỌI chỗ đọc tên re_hung thành ngon_cai (1 chỗ)'


# ── Vòng ba: sau khi cổng với tới được chuỗi, tên, thuộc tính, is/in ─────
# Mở rộng bộ sinh (492 → 949 đột biến) làm lộ thêm một lớp nữa. Phần lớn là
# TƯƠNG ĐƯƠNG THẬT theo một kiểu đặc biệt đáng chú ý: đột biến sống sót
# CHÍNH LÀ luận điểm của bài. `.strip()` ba chuỗi bẩn ra cùng một chuỗi
# sạch; `pathlib` coi có gạch cuối và không gạch là một; hai cách đếm cho
# cùng một số. Ở mấy bài ấy, một cổng BẮT ĐƯỢC đột biến mới là cổng sai.

# TƯƠNG ĐƯƠNG THẬT, và đột biến sống sót CHÍNH LÀ luận điểm của bài: `pathlib` xử lý đường dẫn có gạch cuối và không gạch như nhau, nên đổi hai cái tên ấy cho nhau ra đúng cùng một đường dẫn.
- 'nen-tang.chuong-trinh-that.ghep-duong-dan-cho-dung · ghep-hai-duong-dan-bang-pathlib · đổi MỌI chỗ đọc tên thu_muc_khong_gach thành thu_muc_co_gach (1 chỗ)'

# TÊN FILE KHÔNG KIỂM ĐƯỢC Ở BÀI NÀY. Bước chấm bằng biên nhận của `.write` và nội dung dòng đem ghi; muốn kiểm tên file thì phải ĐỌC file lại, mà đọc là khái niệm của bài 4, và ở bài 1 kết nối còn chưa đóng nên chữ chưa chắc xuống đĩa (luật của bài 2). Chính người viết đã ghi lại ràng buộc này.
- 'nen-tang.chuong-trinh-that.ghi-chu-xuong-file · ghi-dong-dau-cua-so · đổi MỌI chuỗi 'so-thang-tam.txt' thành 'w' (1 chỗ)'

# TƯƠNG ĐƯƠNG THẬT, và là luận điểm của bài: ba chuỗi bẩn khác nhau đều `.strip()` ra đúng "cà phê" dài 6. Đổi chỗ chúng cho nhau tất nhiên ra cùng kết quả.
- 'nen-tang.gia-tri-bien-kieu.khoang-trang-vo-hinh · don-ba-lan-go · đổi MỌI chỗ đọc tên lan_mot thành lan_hai (1 chỗ)'

# TƯƠNG ĐƯƠNG THẬT, và là luận điểm của bài: ba chuỗi bẩn khác nhau đều `.strip()` ra đúng "cà phê" dài 6. Đổi chỗ chúng cho nhau tất nhiên ra cùng kết quả.
- 'nen-tang.gia-tri-bien-kieu.khoang-trang-vo-hinh · don-ba-lan-go · đổi MỌI chỗ đọc tên lan_hai thành lan_mot (1 chỗ)'

# TƯƠNG ĐƯƠNG THẬT, và là luận điểm của bài: ba chuỗi bẩn khác nhau đều `.strip()` ra đúng "cà phê" dài 6. Đổi chỗ chúng cho nhau tất nhiên ra cùng kết quả.
- 'nen-tang.gia-tri-bien-kieu.khoang-trang-vo-hinh · don-ba-lan-go · đổi MỌI chỗ đọc tên lan_ba thành lan_mot (1 chỗ)'

# TƯƠNG ĐƯƠNG THEO DỮ LIỆU. `tong_sang == 30.3` và `tong_chieu == 30.3` đều `False` — cái đầu vì phép cộng số thực lệch (đúng điều bài dạy), cái sau vì hai số vốn khác nhau.
- 'nen-tang.gia-tri-bien-kieu.so-thuc-chi-la-so-xap-xi · hoi-may-hai-cau · đổi MỌI chỗ đọc tên tong_sang thành tong_chieu (1 chỗ)'

# DỮ LIỆU CHƯA PHÂN BIỆT ĐƯỢC. `mot < hai` cho `False`, mà `mot < mot` cũng `False`. ĐÁNG SỬA SAU: xếp lại để `mot < hai` ra `True` thì `mot < mot` lộ ngay.
- 'nen-tang.list-dict-set-tuple.may-nhin-o-dau-truoc · hoi-thang-may-cap-nao-truoc · đổi MỌI chỗ đọc tên hai thành mot (1 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI (bài dạy `or`); mốc 65 là tham số của đề. Khoá miễn trừ cũ hết hạn vì bài vừa được thêm khách thứ hai — đó là ý muốn: bài đổi thì miễn trừ phải xét lại.
- 'onboarding.ra-lenh-cho-byte.chi-can-mot-ve-dung · tam-bien-khuyen-mai · đổi MỌI dấu > thành >= (2 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI (bài dạy `or`); mốc 65 là tham số của đề. Khoá miễn trừ cũ hết hạn vì bài vừa được thêm khách thứ hai — đó là ý muốn: bài đổi thì miễn trừ phải xét lại.
- 'onboarding.ra-lenh-cho-byte.chi-can-mot-ve-dung · tam-bien-khuyen-mai · đổi MỌI hằng số 65 thành 66 (2 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI (bài dạy `and`); mốc 8 giờ là tham số của đề. Khoá cũ hết hạn vì bài vừa thêm khách thứ hai và thứ ba.
- 'onboarding.ra-lenh-cho-byte.hai-dieu-kien-cung-dung · dan-to-giay-len-tuong · đổi MỌI dấu < thành <= (3 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI (bài dạy `and`); mốc 8 giờ là tham số của đề. Khoá cũ hết hạn vì bài vừa thêm khách thứ hai và thứ ba.
- 'onboarding.ra-lenh-cho-byte.hai-dieu-kien-cung-dung · dan-to-giay-len-tuong · đổi MỌI hằng số 8 thành 9 (3 chỗ)'

# TƯƠNG ĐƯƠNG THEO DỮ LIỆU: hai cách đếm cho ra ĐÚNG cùng một con số — đó là toàn bộ luận điểm của bài.
- 'toan.cam-nhan-so.dem-la-ghep-doi-mot-mot · hai-cach-dem-mot-ket-qua · đổi MỌI chỗ đọc tên an_dem_ca_dong_duoc thành byte_dem_ca_dong_duoc (1 chỗ)'

# TƯƠNG ĐƯƠNG THEO DỮ LIỆU: hai người đo cùng một vật bằng cùng một sải nên ra cùng con số — đúng điều bài dạy.
- 'toan.cam-nhan-so.do-la-dem-cai-thuoc · giu-thuoc-hay-doi-thuoc · đổi MỌI chỗ đọc tên byte_do_bang_sai_day thành an_do_lai_bang_sai_day (2 chỗ)'

# TƯƠNG ĐƯƠNG THEO DỮ LIỆU: hai người đo cùng một vật bằng cùng một sải nên ra cùng con số — đúng điều bài dạy.
- 'toan.cam-nhan-so.do-la-dem-cai-thuoc · giu-thuoc-hay-doi-thuoc · đổi MỌI chỗ đọc tên an_do_lai_bang_sai_day thành byte_do_bang_sai_day (1 chỗ)'

# TƯƠNG ĐƯƠNG THEO DỮ LIỆU: hai người đo cùng một vật bằng cùng một sải nên ra cùng con số — đúng điều bài dạy.
- 'toan.cam-nhan-so.do-la-dem-cai-thuoc · giu-thuoc-hay-doi-thuoc · đổi MỌI chỗ đọc tên an_do_bang_buoc_chan thành byte_do_bang_sai_day (1 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI. Bài dạy "so hai số lẻ bằng khoảng cách
# dưới một ngưỡng, đừng dùng ==" — chỗ hoà giữa `<` và `<=` tại đúng biên
# nguong không phải điều bài nói tới. Với dữ liệu của bài, chênh lệch thật do
# số lẻ gây ra (~5.5e-17) không bao giờ chạm khít mốc nguong (1e-9), nên `<`
# và `<=` cho ĐÚNG cùng kết quả trên mọi cảnh bài này dựng — TƯƠNG ĐƯƠNG THẬT,
# giống ca `giu-lai-ky-luc` ở trên.
- 'khoa-hoc-may-tinh.bit-byte-va-bo-nho.boss-ke-mot-cau-chuyen-bo-nho · viet-doi-mot-gia-tri · đổi MỌI dấu < thành <= (1 chỗ)'

# NGƯỠNG KHÔNG PHẢI KHÁI NIỆM CỦA BÀI. `gan_bang` dạy "so chênh lệch với một
# ngưỡng thay vì so bit tuyệt đối bằng ==" — ranh giới biên đúng bằng ngưỡng
# (`<` loại trừ biên, `<=` bao gồm biên) không phải điều bài nói tới, và cả
# năm assert của bài đều kiểm chênh lệch RÕ RÀNG nhỏ hơn hoặc RÕ RÀNG lớn hơn
# ngưỡng, không ca nào chạm khít biên — nên `<` và `<=` cho ĐÚNG cùng kết quả
# trên mọi cảnh bài này dựng. Cùng lớp với `viet-doi-mot-gia-tri` ở trên
# (cùng track, cùng tác giả gốc, cùng lý do).
- 'khoa-hoc-may-tinh.bit-byte-va-bo-nho.hai-so-float-bang-nhau-khong · viet-ham-gan-bang · đổi MỌI dấu < thành <= (1 chỗ)'

# THAM SỐ PHỤ KHÔNG ẢNH HƯỞNG KẾT QUẢ. `muc_tieu=5` chỉ là đối số thứ hai
# truyền vào dem_buoc_hai_vong — so_buoc đếm MỌI lượt của hai vòng lồng
# nhau, không phụ thuộc điều kiện `if mang[i]+mang[j]==muc_tieu`. Đổi 5
# thành 6 không đổi một bước đếm nào, ở bất kỳ cỡ dữ liệu nào — TƯƠNG ĐƯƠNG
# THẬT, chính bài đã nói rõ điều này trong hint ("giá trị nào cũng được").
- 'khoa-hoc-may-tinh.thuat-toan-va-do-phuc-tap.gap-doi-du-lieu-buoc-tang-the-nao · do-bon-co · đổi MỌI hằng số 5 thành 6 (1 chỗ)'

# TƯƠNG ĐƯƠNG THẬT do chính cặp thuật toán được chọn: cách B (O(n)) thắng
# cách A (O(n²)) ở CẢ HAI cỡ dữ liệu bài dùng (n=10: 10<45; n=100: 100<4950)
# — không có cỡ nào khiến A thắng, nên nhánh "A" của biểu thức ba ngôi
# không bao giờ được chọn: đổi chuỗi 'A' thành 'B' không đổi giá trị cuối
# cùng vì nhánh đó chết theo đúng thiết kế bài (bài chỉ hỏi "ai thắng ở dữ
# liệu LỚN", không cam kết có cỡ nào A thắng). Cùng lý do, khoảng cách hai
# số bước đo được (100 với 4950) không bao giờ hẹp tới mức chạm biên </<=.
- 'khoa-hoc-may-tinh.thuat-toan-va-do-phuc-tap.so-sanh-hai-thuat-toan-bang-big-o · ai-thang-o-du-lieu-lon · đổi MỌI dấu < thành <= (1 chỗ)'
- "khoa-hoc-may-tinh.thuat-toan-va-do-phuc-tap.so-sanh-hai-thuat-toan-bang-big-o · ai-thang-o-du-lieu-lon · đổi MỌI chuỗi 'A' thành 'B' (1 chỗ)"

# TƯƠNG ĐƯƠNG THẬT VỀ LOGIC, không phải do thiếu dữ liệu test: nhánh elif
# `danh_sach[giua] < can_tim` chỉ chạy SAU KHI nhánh `if` ngay trên đã loại
# trừ trường hợp bằng nhau (`danh_sach[giua] == can_tim`) — tới được elif
# nghĩa là hai giá trị CHẮC CHẮN khác nhau, nên `<` và `<=` luôn cho đúng
# cùng kết quả tại đây, với MỌI dữ liệu, không riêng ca bài này dựng. Đây
# là hệ quả tất yếu của cấu trúc if/elif hai bài dạy, không phải lỗ thật.
- 'khoa-hoc-may-tinh.thuat-toan-va-do-phuc-tap.tim-nhi-phan · viet-vong-lap-nhi-phan · đổi MỌI dấu < thành <= (1 chỗ)'
- 'khoa-hoc-may-tinh.thuat-toan-va-do-phuc-tap.tim-nhi-phan-bang-de-quy · viet-de-quy-nhi-phan · đổi MỌI dấu < thành <= (1 chỗ)'

# THAM SỐ PHỤ KHÔNG ẢNH HƯỞNG KẾT QUẢ, cùng lớp `do-bon-co` ở trên. `100`
# chỉ cần là MỘT GIÁ TRỊ KHÔNG CÓ MẶT trong mang_tam=[7,2,9,4,1,8,3,6] —
# hint của chính bài xác nhận "hoặc bất kỳ số nào khác không xuất hiện".
# Đổi 100 thành 101 (cũng không có mặt) cho ra hệt cùng số bước (8) và vị
# trí (-1) — TƯƠNG ĐƯƠNG THẬT.
- 'khoa-hoc-may-tinh.thuat-toan-va-do-phuc-tap.truong-hop-xau-nhat · do-hai-ca-cung-mot-danh-sach · đổi MỌI hằng số 100 thành 101 (1 chỗ)'

# THAM SỐ PHỤ KHÔNG ẢNH HƯỞNG KẾT QUẢ — cùng lớp `do-bon-co`/`do-hai-ca-cung-
# mot-danh-sach` ở trên. "zero la gia tri hop le" là tham số THỨ BA của
# assertEqual (label, chỉ dùng để IN thông điệp lỗi khi assertion thất bại)
# — KHÔNG tham gia phép so sánh actual/expected nào cả. Ba blank học viên
# điền (0/null/100.01, giá trị MONG ĐỢI) hoàn toàn độc lập với nội dung
# nhãn — đổi nhãn thành "0" không đổi PASS/FAIL của bất kỳ assertion nào.
# Bài dạy liệt kê trường hợp biên (giá trị mong đợi), không dạy nội dung
# nhãn mô tả.
- 'ky-nghe-phan-mem.kiem-thu.truong-hop-bien-co-y-thuc · viet_them_bien_parseprice · đổi MỌI chuỗi "zero la gia tri hop le" thành "0" (1 chỗ)'

# THAM SỐ PHỤ KHÔNG ẢNH HƯỞNG KẾT QUẢ — cùng lớp ca ngay trên. "nhap khong
# the nhay thang toi giao_hang" là tham số THỨ BA của assertDeepEqual
# (label, chỉ dùng để IN thông điệp lỗi khi assertion thất bại) — KHÔNG
# tham gia phép so sánh actual/expected nào cả. Blank học viên điền (giá
# trị của field `tag`, "loi") hoàn toàn độc lập với nội dung nhãn — đổi
# nhãn thành "nhap" không đổi PASS/FAIL của assertion. Bài dạy các chuyển
# đổi KHÔNG hợp lệ phải bị từ chối, không dạy nội dung nhãn mô tả.
- 'ky-nghe-phan-mem.kiem-thu.test-chuyen-trang-thai-tu-choi · viet_test_chuyen_tu_choi · đổi MỌI chuỗi "nhap khong the nhay thang toi giao_hang" thành "nhap" (1 chỗ)'

# THAM SỐ PHỤ KHÔNG ẢNH HƯỞNG KẾT QUẢ — cùng lớp hai ca ngay trên. "nen tra
# ve loi khi email thieu @" là tham số THỨ BA của assertDeepEqual (label,
# chỉ dùng để IN thông điệp lỗi khi assertion thất bại) — KHÔNG tham gia
# phép so sánh actual/expected. Blank học viên điền (giá trị Result mong
# đợi) hoàn toàn độc lập với nội dung nhãn. Bài dạy cấu trúc Arrange-Act-
# Assert và đặt tên, không dạy nội dung nhãn mô tả.
- 'ky-nghe-phan-mem.kiem-thu.arrange-act-assert-va-dat-ten · viet_test_aaa · đổi MỌI chuỗi "nen tra ve loi khi email thieu @" thành "email_khong_hop_le" (1 chỗ)'

# TƯƠNG ĐƯƠNG THẬT. Bài này khai CẢ HAI assertEqual (so sánh ===) lẫn
# assertDeepEqual (so sánh qua JSON.stringify) trong cùng phạm vi — vì
# blank1 CẦN assertDeepEqual (Result là object, so === sẽ luôn false do
# khác tham chiếu) còn blank2 dùng assertEqual (so một con số). MỌI lời
# gọi assertEqual trong bài (daLuu.length, ketQuaRieng.kind, ketQuaRieng.loi)
# đều so sánh giá trị NGUYÊN THUỶ (number/string) — với kiểu dữ liệu này,
# JSON.stringify(a) === JSON.stringify(b) và a === b luôn cho ra CÙNG một
# kết quả true/false (không có object/mảng nào lọt vào so sánh qua
# assertEqual ở bài này). Đổi tên hàm gọi từ assertEqual sang assertDeepEqual
# do đó không đổi PASS/FAIL của bất kỳ assertion nào — không có test nào
# tách bạch được hai hàm khi cả hai chỉ nhận nguyên thuỷ. Sửa thật đòi hỏi
# đổi hẳn khái niệm bài (ép một blank nhận object qua assertEqual) — lệch
# khỏi ý bài (Arrange-Act-Assert + đặt tên, không phải phân biệt hai hàm
# assert).
- 'ky-nghe-phan-mem.kiem-thu.arrange-act-assert-va-dat-ten · viet_test_aaa · đổi MỌI chỗ đọc tên assertEqual thành assertDeepEqual (1 chỗ)'

